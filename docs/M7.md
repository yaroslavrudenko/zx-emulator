# M7 — the 128

Design for `crates/spectrum`. [`MACHINE.md`](MACHINE.md)'s milestone table says **128: paging,
second ROM, AY-3-8912, per-bank contention — gate: 128-only software runs.** Decision 8 corrects
the second half of that sentence.

Each decision carries the class of evidence behind the claim that forced it, in the vocabulary
[`M6.md`](M6.md) established — **proven**, **measured**, **derived**, **observed**, plus **ruling**
for a choice no evidence forces, recorded so it is not re-litigated.

> **Every port bit, frame length, contention figure and AY register width below is a pointer to
> verify, not a specification.** They are written from familiarity with the hardware, not
> transcribed from it in front of the implementer. `M6.md` opened with the same warning and it was
> **right**: three of its numbers were later found wrong, and one of them — the `.z80` compression
> rule that a literal `ED` is never followed by an escape — was a defect **no round trip could
> have caught, because our own decoder misread it symmetrically**. A number in this document that
> disagrees with a source is a defect in this document. The implementer transcribes; this document
> only says which numbers the *design* turns on, and therefore which ones are worth the
> transcription.
>
> The figures at highest risk, named so they are checked first: **the `0x7FFD` bit layout and its
> partial address decode** (Decision 2), **`FIRST_CONTENDED_T_STATE` for the 128** (Decision 4),
> **the AY's per-register write masks and its noise polynomial** (Decision 6), and **the `.z80`
> hardware-mode numbers for the 128 in each of versions 2 and 3** (Decision 7) — the last because
> `M6.md` already found that the two versions *renumber* the modes and that accepting mode 3
> unconditionally *"would load a 128 snapshot as a 48K and silently lose five banks."*

---

## What M7 inherits, and the one thing it does not

**Most of this milestone was paid for at M5.** [`ARCHITECTURE.md`](ARCHITECTURE.md) Decision 5 built
paged memory two milestones early — *"48K is a special case of the 128: paging locked, banks
fixed. Cost: about thirty lines."* Decision 1 below **counts** what that bought — in fields and
methods, not in adjectives — because a claim about a saving is worth as little as any other
uncounted claim.

**M6 built the instrument M7's state needs.** `Snapshot` is bank-keyed rather than address-keyed,
and `M6.md` Decision 2 gives the reason in the present tense — the 48K's three `.z80` page numbers
*"are neither contiguous nor derivable from the 128's rule"* — with *"that M7 needs it anyway"*
explicitly demoted to a bonus. It is now the requirement, and the design is already the right shape.

**What M7 does not inherit is an oracle.** M6 had one for the *files*: a `.z80` we write either
loads in somebody else's emulator or it does not. M5 ended without one for the *machine*, and then
got one — `crates/spectrum/tests/timing_oracle.rs`, 70 hardware-measured numbers, 0 disagreements.
**Whether a 128 equivalent exists is the first question in this milestone**, because the answer
decides what M7's gate can be and whether its contention figures are transcribed or established.
The survey is Decision 8; Decision 4 says what turns on it.

---

## Decision 1 — one machine, and a 48K is a 128 that powered on with the lock already set — **derived**

The candidates were a configuration of `Spectrum`, a sibling type, and a trait.

### What actually differs

| | 48K | 128 | Where it lands |
|---|---|---|---|
| RAM | 3 banks reachable | 8 banks, slot 3 pageable | `Memory::slots` — **already a slot map** |
| ROM | one page | two: 128 editor, 48 BASIC | `ROM_COUNT` — **already 2** |
| Contended | bank 5 | banks 1, 3, 5, 7, in *any* slot | `Memory::contended` — **already a `[bool; 8]` field** |
| Screen | bank 5 | bank 5 or shadow bank 7 | `screen::render` — a real change, Decision 3 |
| Paging port | — | `0x7FFD` | one new method, Decision 2 |
| Sound | beeper | + AY-3-8912 | a new peripheral, Decision 6 |
| Frame | 69888 T | 70908 T | `Clock` — a real change, Decision 4 |
| First contended T-state | 14335 | different, and **the value is the milestone's open question** | Decision 4 |

Four of the eight rows are **already fields on types that exist**. That is Decision 5's payoff and
it is checkable rather than aesthetic: the diff to `crates/spectrum/src/memory.rs` for the whole
memory half of the 128 is *two `u8` fields and two methods*, derived below.

### The finding that collapses the memory half

`SPECTRUM_48K_SLOTS` is `[Rom(0), Bank(5), Bank(2), Bank(0)]`. Derive a slot map from a `0x7FFD`
value the way a 128 does — bits 0–2 select the bank at `0xC000`, bit 4 selects the ROM at `0x0000`,
slots 1 and 2 are wired to banks 5 and 2 on both machines — and ask which value produces that map:

```text
  0x20 = 0b0010_0000
         bits 0-2 = 000  ->  bank 0 at 0xC000     matches
         bit  3   = 0    ->  screen is bank 5     matches
         bit  4   = 0    ->  ROM page 0           matches
         bit  5   = 1    ->  paging locked        matches: a 48K cannot page
```

**A 48K's memory map is exactly what port value `0x20` derives, and its inability to page is
exactly the lock bit already being set.** So `Memory` needs no model discriminator at all. It needs
the port byte and the value to restore on reset:

```rust
struct Memory {
    ram: Box<[[u8; PAGE_SIZE]; BANK_COUNT]>,
    rom: Box<[[u8; PAGE_SIZE]; ROM_COUNT]>,
    slots: [Slot; SLOT_COUNT],
    contended: [bool; BANK_COUNT],
    paging_port: u8,          // new: the SSOT for slots, screen, ROM and the lock
    paging_port_at_reset: u8, // new: 0x20 for a 48K, 0x00 for a 128
}
```

`write_paging_port` returns early while `paging_port & LOCK != 0`, so **a 48K absorbs a write to
`0x7FFD` with no model check anywhere** — and the existing test
`writing_a_port_the_ula_does_not_own_leaves_the_border_alone`, which writes `0x7FFD` to a 48K and
asserts nothing moved, keeps passing unchanged. `reset` assigns `paging_port_at_reset` and re-derives
the map, which is correct on both machines for the same reason.

This is the strongest single argument in the document and it is worth stating as such: **the
sentence "48K is a configuration of the 128" stops being a slogan and becomes an equation.**

### Why not a sibling type, and why not a trait

**A sibling `Spectrum128` duplicates the frame loop**, and the frame loop is the part of this crate
with the subtlest rule in the project: `MACHINE.md` Decision 1 (the bus owns the clock, never the
sum of `step()` returns) and Decision 2 (one step can overrun the frame, so watch the counter and
never try to land on the budget). `lib.rs` carries a test whose whole job is to prove the first of
those — `the_frame_loop_is_driven_by_the_bus_and_not_by_step_returns` — and a second copy of that
loop is a second place for it to be got wrong, with the copy's failure invisible until a 128 game
runs 20 % long with nothing failing. It also doubles every test helper in `tests/common/mod.rs`.

**A trait, or a `Ula<M: Model>` generic, is worse than either.** It buys runtime polymorphism
nothing needs — a machine does not change model mid-run — and pays for it by monomorphising the
entire `Cpu<Ula<M>>` instantiation twice. `ARCHITECTURE.md`'s codegen section measures that
instantiation (three jump tables of 124 + 119 + 64, 15 bounds checks, one indirect call), and
doubling it to express a two-element set is exactly the over-engineering a two-variant enum
answers for free.

### What it costs callers

| | |
|---|---|
| `Spectrum::new(&rom)` | **unchanged.** Still a 48K, still one ROM |
| `Spectrum::spectrum_128(&editor, &basic)` | new |
| `Spectrum::restore(&Snapshot)` | **becomes fallible** — the one breaking change, argued in Decision 7 |
| every existing 48K test | **unchanged**, which Decision 4 exists to guarantee |

There are ten `Spectrum::new` call sites, all in this workspace, and `crates/frontend/src/main.rs`
is `fn main() {}`. The break is therefore cheap in practice — and it is still a semver event, and
it is still listed and needs approval, because *"adding public fields is free with zero consumers
and breaking with one"* and the consumer count is a fact about today.

---

## Decision 2 — the port value **is** the paging state — **ruling**, with one structural consequence

### The layout

| Bit | Meaning | On a 48K |
|---|---|---|
| 0–2 | RAM bank paged into `0xC000` | — |
| 3 | screen: 0 = bank 5, 1 = shadow bank 7 | — |
| 4 | ROM: 0 = 128 editor, 1 = 48 BASIC | — |
| 5 | **paging disable** — once set, every further write is ignored until reset | set at power-on |
| 6–7 | unused on a 128 (the +2A/+3 use `0x1FFD` instead) | — |

Every row above is confirmed verbatim against the
[World of Spectrum *128K Technical Information*](https://worldofspectrum.org/faq/reference/128kreference.htm)
reference, including bit 5: *"If set, memory paging will be disabled and further output to this
port will be ignored until the computer is reset."*

**The address decode is confirmed from the same source and it is partial**, which is the part that
produces "works here, not there" bugs:

> *"the hardware will respond to any port address with bits 1 and 15 reset."*

So **A15 = 0 and A1 = 0**, and nothing else is decoded — the port answers at a large family of
addresses and not only at `0x7FFD`. Two things follow:

- `ula.rs` already decodes the ULA on **A0 = 0 alone** (`ULA_PORT_SELECT`), and its module
  documentation explains why that is not a simplification: *"this is why the port is written `0xFE`
  but responds at `0x7FFE`, `0xBFFE` and every other even address."* The same reasoning applies
  here, and the two decodes **overlap**: an address with A0 = 0, A1 = 0 and A15 = 0 is claimed by
  both the ULA and the paging port, and on the hardware both respond. The `out_port` arm must
  therefore be **two independent `if`s, never a `match` on the port**, or one device silently
  stops answering.
- Software that pages with `LD BC,0x7FFD : OUT (C),A` is the common case and any decode gets it
  right. Software that reaches the port some other way is what distinguishes a correct decode from
  a lucky one, and it is the reason to write the mask rather than an equality.

### Three partially-decoded devices on one bus, and where they collide

M7 puts a third device on a bus where every device decodes only a few address lines. Writing the
three rules next to each other is the cheapest way to see the interactions, and two of them are
real:

| Device | Responds when | Overlaps |
|---|---|---|
| ULA (`0xFE`) | A0 = 0 | paging, AY |
| Paging (`0x7FFD`) | A15 = 0, A1 = 0 | ULA |
| AY select / read (`0xFFFD`) | A15 = 1, A14 = 1, A1 = 0 | ULA |
| AY write (`0xBFFD`) | A15 = 1, A14 = 0, A1 = 0 | ULA |

- **Paging and the AY cannot collide**: they disagree on A15, always. That is a genuine structural
  guarantee and it is worth asserting once so nobody re-derives it.
- **Paging and the ULA collide** on any address with A0 = 0, A1 = 0, A15 = 0. On a write both act,
  which is well defined and is why the `out_port` arm must be two independent `if`s rather than a
  `match` on the port.
- **The AY and the ULA collide** on any address with A0 = 0, A1 = 0, A15 = 1, A14 = 1. On a *read*
  that is two devices driving the data bus at once, and the result is a hardware conflict with no
  clean model. **`in_port` must therefore fix an order and say so at the site**, and the choice is
  a ruling rather than a measurement until some software is observed to depend on it. There is no
  right answer to be discovered by thinking harder about it.

`ula.rs`'s existing `in_port` returns `FLOATING_BUS_BYTE` for every port with A0 set, which today
includes `0xFFFD`. The AY read arm therefore goes **before** that fallback, not after it — an
ordering bug here reads as "the AY is write-only", which is silent and plausible.

### No value is hostile, and that is a property rather than a hope

This crate builds with `panic = "abort"` in release, so a guest-triggered panic kills the process —
and this project has shipped exactly that defect once already (`M6.md` Decision 6 records the `u8`
T-state accumulator that a 63-byte prefix run overflowed). A paging port is guest-controlled input
by definition: any of 256 values, at any moment, from any program.

**All 256 are legal and none of them can panic**, and the reason is that `M5` already paid for it.
`BankIndex::new` masks to three bits and `RomIndex::new` to one, on construction *and* at the point
of use, and `memory.rs` gives the reason in the present tense: *"on a 128, bits 0–2 of port
`0x7FFD` are the bank number, so every three-bit value is a legal selection and there is nothing to
reject."* That sentence was written about hardware the machine did not yet have. It is now the
specification, and the assertion that discharges it is a loop over all 256 values checking that the
machine is still coherent — **exhaustive on the axis that matters, and cheap**, which is the
combination `M6.md` Decision 6 used for the truncation sweep.

There is no `PagingError`. Adding one would be a type asserting a failure mode that does not exist,
which `STATUS.md` catalogues as *"the type-level form of a comment claiming a protection that does
not exist."*

### The lock is absorbing, and a restore must go around it

Once bit 5 is set, `0x7FFD` is dead until a hard reset. That is one line in `write_paging_port` and
one line in `reset`.

**It is also a trap for the snapshot applier, and it is the same trap `set_border` already
documents.** A `.z80` v3 records the last value written to `0x7FFD` — lock bit included — so a
snapshot of a locked machine restores a locked machine, correctly. But `restore` cannot get there
by *calling* `write_paging_port`, because the machine being restored into may already be locked
from whatever it was running before, and the write would be silently ignored. The applier needs
`pub(crate) fn set_paging_port(&mut self, value: u8)` that assigns and re-derives, bypassing the
lock — exactly as `Ula::set_border` bypasses `out_port` and for a directly analogous reason: a
restore is not a machine cycle, and routing state-setting through a guest-facing path charges
effects that never happened.

`M6.md`'s note that a restore routed through `out_port` *"leaves the round trip green and turns the
contention-after-restore assertion red"* is the template for the gate here: **restore into a locked
machine, then assert the paging took.** A restore that silently did nothing is otherwise invisible,
because every field the round trip compares would still match.

### Reset acquires a job it does not have today

`Spectrum::reset` reaches `Ula::reset`, which touches the clock, the border and the cycle
bookkeeping and **deliberately does not touch `Memory` at all** — *"a reset button does not clear
RAM, does not lift the keys, and does not rewind a cassette."* That is still right about RAM, and
it is now incomplete: **reset clears the paging lock and restores the power-on map**, which is the
only way a locked 128 ever pages again.

So `Ula::reset` gains a call to `Memory::reset`, and that call is **a no-op on a 48K by
construction** — `paging_port_at_reset` is already `0x20`, which is already the value. The existing
`reset_returns_the_clock_to_the_start_without_disturbing_memory`, which writes `0x8000` and asserts
the byte survives, keeps passing unchanged, and it is worth running it deliberately rather than
noticing later that it still does. The AY is reset in the same place and for the same reason.

### One representation, derived once

`Memory` stores the **port byte** and derives `slots`, the screen bank and the lock from it. It
never stores both. The alternative — keeping a `paging_locked: bool` and a `screen_bank: BankIndex`
alongside the byte — is three representations of one datum that can disagree, and the snapshot
writer would then have to choose which to serialise. `slots` is a derived cache and is rebuilt by
the one function that reads the byte, so there is exactly one place where the bit layout is
written down.

---

## Decision 3 — contention follows the bank; the screen follows the port — **derived**

### The hot path does not change

`Memory::is_contended(address)` already asks the slot map rather than the address range, and
`memory.rs` says why in a sentence written at M5: *"Getting this wrong is not a bug that shows up
as a crash; it shows up as a demo tearing three years later."* It already carries the test that
proves it — `contention_follows_the_bank_and_not_the_address_range` moves bank 5 into an
uncontended slot and asserts contention moved with it, and its own comment says it was written
*"at M5 while it is still cheap to fix."*

So the whole of per-bank contention is **one array literal**: `contended` becomes
`[F, T, F, T, F, T, F, T]`. `Ula::tick`, `Ula::contend` and `Ula::port_delay` are unchanged, and
**the branch count on the hot path does not move** — which matters, because `tick` is the
emulator's hottest function and its branches were deliberately reduced when the machine-cycle
heuristic was retired.

> **One honest cost, stated rather than glossed.** Decision 4 turns `delay()`'s inputs from
> compile-time constants into fields of a `Timing` value, so the frame length, the first contended
> T-state and the pattern stop being immediates folded into the instruction stream and become loads
> from a struct that lives inside `Ula`. Same branches, a few more loads, from a line that is hot by
> construction because `tick` runs every T-state. **This is a change to the hot path and calling it
> "byte-identical" would be an unmeasured claim of exactly the kind `ARCHITECTURE.md`'s *Measured*
> section exists to catch.**
>
> It is affordable and the affordability is already measured, not assumed: the paged, contended bus
> runs at ~160× real time and spends **~125 µs of a 20,000 µs frame — 0.6 % of the budget** — and
> `ARCHITECTURE.md`'s standing position is that performance is a non-goal and nothing is optimised
> until measured. Nobody should chase the delta, and nobody should assert it is zero either.
>
> > **This named `cargo bench -p z80 --bench step` as the command, and that command cannot see
> > this change at all.** Checked on 2026-09-01: `crates/z80/benches/step.rs` imports exactly
> > `divan` and `z80` — no `spectrum`, no `Ula`, no `Timing` — and defines its **own** stand-in
> > paged bus, `impl Bus for FlatRam` at `:129`. `crates/z80/Cargo.toml` does not depend on
> > `crates/spectrum` (its one occurrence of the word is a comment). So the bench measures a CPU
> > against a bus this milestone does not touch, and **there is no benchmark in this workspace
> > that exercises `spectrum::Ula`**: `find crates -path '*/benches/*'` returns that one file, and
> > `crates/spectrum/Cargo.toml` has no `[[bench]]` and no bench harness.
> >
> > **So the honest statement is that the command to measure this does not exist**, and naming
> > one that does exist but measures something else is worse than naming none: it is checkable,
> > it runs, it produces numbers, and every one of them is about a different bus. That is
> > `STATUS.md`'s *"a count is a property of the probe, and the probe is part of the claim"* — a
> > probe pointed at the wrong subject reports a real measurement of the wrong thing.
> >
> > **What would settle it:** a bench in `crates/spectrum` that steps the real `Ula` over a frame,
> > which is the same shape `step.rs` already has and does not exist yet. Until somebody writes
> > it, the ~160× and 0.6 % figures above stand as what they are — measurements of the machine
> > taken elsewhere and cited here, not output of a command this paragraph can hand you.

### The case a 48K never has, and a gate that already anticipates it

On a 128 a contended bank can be paged into `0xC000`, so **the stack, the refresh address `IR`, and
the program counter can all sit in contended memory at once** — a configuration a 48K cannot
reach, because its only contended bank is nailed to `0x4000`.

`STATUS.md` already names this: the internal-run case *"requires arriving from an uncontended cycle
onto a contended address, which happens only when the internal cycles ride the refresh address
`IR`… **M7 makes that case routine, because a 128 contends banks in whichever slot they are paged
into.**"* `contention_magnitude.rs` grades it today for one arrangement. The M7 addition is not a
new mechanism — it is the same gate run with a contended bank at `0xC000`, and it is close to free.

### The screen the ULA actually draws

`screen::render` reads through `Memory::read(DISPLAY_FILE + offset)`. On a 128 that is wrong in a
way that is invisible until bit 3 of `0x7FFD` is set: **the ULA draws from bank 5 or bank 7
directly, not through the slot map**, and bank 7 usually has no address at all.

The fix reads from the bank the port selects, which is *the same code on both machines* because a
48K's screen bank is 5 and bank 5 is at `0x4000`:

```rust
pub fn render(memory: &Memory, border: Colour, flash_phase: bool, frame: &mut Frame)
//            ^ unchanged signature: `Memory` already knows which bank the ULA is drawing
```

**`screen::render`'s public signature does not move**, and neither do `DISPLAY_FILE`,
`ATTRIBUTE_FILE` or any other public constant, because they are still the true addresses at which
bank 5 appears. What changes is one indirection inside the function. This is the second independent
caller for `Memory::bank`, and Decision 7 shows the first.

Three properties to gate, because each is a plausible independent wrong answer:

- switching bit 3 changes what `render` draws, **and changes nothing else** — not the slot map, not
  contention, not what `Memory::read(0x4000)` returns;
- **the contended set does not follow the screen select.** Banks 1, 3, 5 and 7 are contended
  whichever one the ULA is drawing. A model that contended "the screen bank" would be right for
  bank 5 and wrong for the other three, and would pass any test that only ever selects bank 5;
- writing to bank 7 while it is paged into `0xC000` **and** selected as the screen is visible on
  the display, which is the whole reason the shadow screen exists.

---

## Decision 4 — the frame's geometry is a value, the 48K's constants do not move, and the 128's number is the milestone's open question

This decision has two halves. The first is a design choice with a measured cost. The second is a
research result and it is the most important paragraph in the document.

### The number

The 128's frame is **70908 T-states**, and unusually for this project that figure has two
independent transcriptions in the repository that agree. `MACHINE.md`'s 48K/128 table states it,
and the `.z80` format description states the T-state counter's low word *"counts down from 17471 to
0 (17726 in 128K modes), which make a total of 69888 (70908) T states per frame"* — quoted in
`M6.md` Decision 7 for an unrelated purpose. `(17726 + 1) × 4 = 70908` exactly, and
`70908 = 228 × 311`. Two sources, one arithmetic identity, agreeing. That is better than most
figures here start out.

**`FIRST_CONTENDED_T_STATE` for the 128 is a different matter and is not settled by this
document.** The figure in circulation is 14361. What this repository knows about it is precisely
one thing, and it is easy to over-read:

> `timing_oracle.rs`'s mutation table has a row *"`FIRST_CONTENDED_T_STATE` 14335 → 14361 (the
> 128's figure) — **RED, 23 of 68**"*.
>
> **That establishes that 14361 is not the 48K's number. It is not evidence that it is the 128's.**
> The two claims share a numeral and nothing else. A mutation that reddens a 48K oracle tells you
> about a 48K; it is silent about a machine the oracle does not describe. Writing it up as
> *"the strongest evidence available that the two machines are genuinely different"* is the correct
> reading — the machines **are** different, by 23 rows — and the step from there to *"and 14361 is
> what the 128 uses"* is unsupported. Distinguishing those two readings is the entire content of
> this paragraph, and `STATUS.md` catalogues the failure of not distinguishing them as
> *"an argument that names real quantities, predicts the right verdict, and identifies the wrong
> cause."*

The derivation does not transfer either. The 48K's is `64 × 224 − 1 = 14335`; the 128's would have
to be `63 × 228 − 3 = 14361`, which is a different offset from the line boundary (−3, not −1) and a
different pre-display line count. **A derivation whose *shape* changes between two machines is not
a derivation, it is a fit.** And the phases differ: `14335 mod 8 = 7` against `14361 mod 8 = 1`, so
the pattern's alignment within its eight-T-state group is not preserved either.

**And the source is thinner than it looks.** The survey in Decision 8 found 14361 in two places —
the [World of Spectrum *128K Technical Information*](https://worldofspectrum.org/faq/reference/128kreference.htm)
page (*"the 6,5,4,3,2,1,0,0 pattern starts at 14361 T states after the interrupt"*, *"repeats every
228 T states"*, *"Memory banks 1,3,5 and 7 are contended"*) and the
[Sinclair Wiki](https://sinclair.wiki.zxnet.co.uk/wiki/Contended_memory), which says the same thing.
**The wiki page cites no source at all for the 128 figures and reads as a descendant of the FAQ**,
so this is one ancestor seen twice rather than two witnesses — the shape `STATUS.md` names as
*"a derived figure repeated across documents acquires authority it never earned."* No primary
posting or measurement for 14361 was found.

The one thing the source does well: it states the figure **relative to the interrupt**, which is
exactly and only the frame the 48K oracle established as meaningful. So the transcription needs no
reinterpretation, and reinterpretation is where a silent error would live.

**So the 128's contention phase is exactly where the 48K's was before `timing_oracle.rs` landed,
and everything `STATUS.md` says about that state applies verbatim:** *"a gate that fails when a
constant changes says nothing about whether the constant was right."* Decision 8 says what would
change it, and how far — which is not all the way.

### The design: `Timing` is a value, and nothing existing moves

`timing::T_STATES_PER_FRAME`, `FIRST_CONTENDED_T_STATE`, `INTERRUPT_T_STATES`, `T_STATES_PER_LINE`,
`LINES_PER_FRAME` and `delay()` are `pub`. The obvious move is to replace them with model-aware
equivalents. **It is measurably the wrong move**, and this is the one place in the document where
the cost was counted rather than felt:

```sh
grep -rln 'FIRST_CONTENDED_T_STATE\|T_STATES_PER_FRAME\|INTERRUPT_T_STATES' crates/spectrum/tests/ | wc -l
ls -1 crates/spectrum/tests/*.rs | wc -l
```

On 2026-09-01 that was **14 of 19**, and in every case the reference is the positioning device —
`advance_to(&mut machine, FIRST_CONTENDED_T_STATE + phase)`. Each of those gates is *correct for a
48K* and each one says so in its own header. Changing the constants means rewriting fourteen
correct test files to express a 48K fact in a more general vocabulary: churn with a defect budget
and no gate to show for it.

> **Run the command; do not quote the ratio.** It read **13 of 16** when this paragraph was first
> drafted and **14 of 19** roughly twenty minutes later, because other agents were landing M6's
> tests in the same working tree throughout. The *design* conclusion is not sensitive to the exact
> figure — it is that these constants are the positioning device for most of the suite, and that
> ratio is going up, not down. The number is recorded with its date because a bare integer here is
> the same defect `MACHINE.md`'s milestone table just had corrected out of it, and watching it
> change mid-paragraph is the cheapest possible demonstration of why.

So: **the constants stay, unchanged, as the 48K's — and they are redefined as projections of a new
value type, so there is still one source.**

```rust
/// A machine's frame geometry: everything that differs between a 48K and a 128.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct Timing { /* line length, lines per frame, first contended T-state,
                      interrupt window, delay pattern */ }

impl Timing {
    pub const SPECTRUM_48K: Self = /* ... */;
    pub const SPECTRUM_128: Self = /* ... */;
    pub const fn delay(&self, frame_t_state: u32) -> u32 { /* today's `delay`, parameterised */ }
}

pub const T_STATES_PER_FRAME: u32 = Timing::SPECTRUM_48K.frame_t_states();
pub const FIRST_CONTENDED_T_STATE: u32 = Timing::SPECTRUM_48K.first_contended_t_state();
// ...and so on: derived, not repeated.
```

**SSOT is then enforced by the compiler rather than by discipline**, which is the same instrument
`memory.rs` and `timing.rs` already use for their power-of-two assumptions (`const _: () =
assert!(...)`). The free `delay(frame_t_state)` keeps its signature and becomes
`Timing::SPECTRUM_48K.delay(frame_t_state)` — a one-line body change, no public signature moved,
**zero public removals**.

`Clock` carries a `Timing` by value. `Clock::new()` keeps its signature and yields the 48K clock,
which is what it already means. `Clock` grows from 16 bytes to roughly 32; it is `Copy`, it is
returned by value from `Ula::clock()`, nothing hot copies it, and nothing asserts its size.

Two knock-on effects that must be handled rather than discovered:

- **`snapshot/z80.rs`'s quarter arithmetic is model-dependent, and it is a function of the model.**
  `quarter_t_states(model)` is `model.timing().frame_t_states() / QUARTERS_PER_FRAME`, and the
  compile-time assertions pin both machines rather than one — 17472 and 17727, and each of them
  times four is 69888 and 70908. The assertion this replaces,
  `QUARTER_T_STATES * QUARTERS_PER_FRAME == T_STATES_PER_FRAME`, was true on both machines whatever
  frame length it was handed, so **it could not detect the frame length being wrong for the model**;
  a single constant satisfied it and misplaced every 128 file written through it. `M6.md` named this
  field *"the single field where a confident-sounding formula is most dangerous"*, and its
  exhaustive-over-69888 test is exhaustive over both frame lengths: `MODELS` is
  `[Spectrum48K, Spectrum128]` and all five gates that grade the counter loop over it.
  `the_counter_matches_the_format_descriptions_own_sentence` works six positions out by hand per
  machine and then asserts the two encodings of frame position 0 **differ**, which is the assertion
  a single-model loop cannot make and the reason the old one stayed green.
  `the_counter_agrees_with_libspectrum_over_the_whole_frame` transcribes the FUSE library's
  expressions with `quarter_states` left as the parameter of the machine it is there, and
  `the_counter_survives_every_frame_position` sweeps 69888 **and** 70908 positions. Both expected
  values are read out of the format description's own sentence rather than out of
  `quarter_t_states`, so no gate is graded by its subject. The other two are
  `every_counter_a_file_can_hold_decodes_to_a_frame_position`, which folds every `(low, high)` a
  file can hold into the model's own frame, and `the_high_counter_is_read_modulo_four`. The
  module's remaining tests do not
  loop and are not asked to: the version-to-model table, the additional header's length and the
  counter's byte offsets are claims about the *format*, identical on both machines, and the one
  other model-parameterised gate, `the_mode_the_writer_emits_is_the_mode_the_reader_reads_back`,
  carries its own two-element array. **That sentence read *every* gate until it was checked
  against the module** — written, at that, during the correction pass for this very defect,
  which is the habit worth naming: prose summarising a gate drifts toward the stronger claim,
  because a green run never contradicts it.
- **`tape/tap.rs` derives its inter-block pause from `T_STATES_PER_FRAME`**, deliberately, *"the
  machine's own constant rather than the clock speed."* On a 128 that yields a pause 1.5 % longer.
  It is far inside the ROM loader's tolerance and it is named here so it is not later found and
  mistaken for a bug. That one is a derivation held on purpose; the one above it was not.

**The first of those two was named in this document before it was written into the code, and it
shipped anyway — which is a stronger finding than the defect.** The instruction was not vague. It
named the file, the constant, the reason a green assertion would prove nothing, and the test that
had to change. It printed **17727** in its own parenthesis. And nothing was left to look up:
`timing.rs`'s `the_frame_length_agrees_with_the_z80_formats_own_arithmetic` had already transcribed
`(17726 + 1) × 4 == 70908` out of the very sentence the writer then read half of — so the 128's
quarter was in the repository, in a passing test, before `T_STATES_PER_FRAME / 4` was applied to
both machines. The cost is **measured**, not estimated: a 128 standing at 69888 or beyond reduced
into the 48K frame and came back as 0, and every surviving position was displaced by exactly 255,
510, 765 or 1020 T-states — 17472 positions each, and never zero. Our reader divided by the same
wrong quarter, so R2 and R3 and the third-party corpus all stayed green; the displacement was
visible only to a reader that had read the whole sentence.

**So the instruction was right and the following-through was the failure — but the shape of the
instruction is what made skipping it cost nothing.** It asked for a future edit to a test
(*"must become exhaustive over both frame lengths"*), and a request to a later reader is satisfied
by a reader who agrees with it and then does something else. Nothing in the tree went red while it
was outstanding, because the thing it was protecting against was invisible to every gate that
existed. What closed it in the end was not a better sentence: it was `MODELS` as a two-element
array that every gate iterates, and `quarter_t_states(model)` taking the model as an argument so
that a caller cannot fail to supply one. **Prose can name a defect; only a type or a gate can
refuse it.** Where this document says a test *must become* something, the cheaper form is to say
what shape makes the wrong version impossible to write — and Decision 1's `paging_port_at_reset`
and Decision 2's `BankIndex::new` are already that form, which is why neither of them needs a
paragraph like this one.

---

## Decision 5 — the interrupt acknowledge: the 128 does **not** make it decidable, and something that already exists does

`ula.rs` records a known model defect and hands it to this milestone:

> *"An interrupt acknowledge has no callback — it reads no memory, asserting `/IORQ` in place of
> `/MREQ` — so `crates/z80` delivers it as seven bare `Bus::tick` calls at the refresh address…
> The hardware performs **one** machine cycle there — M1 stretched by two wait states — so a
> contended `IR` would be charged seven stalls where it owes one.*
>
> ***It is unobservable on a 48K, and that is why it is written down rather than fixed.*** *…What
> makes this worth recording now: the 128 has a different frame geometry, and `docs/STATUS.md`
> already lists interrupt acceptance as having **no oracle in this project at all**. This is the
> shape that gap has."*

### The 128's geometry does not deliver, and the arithmetic is short

The acknowledge is unobservable on a 48K because `/INT` is asserted for the first 32 T-states of
the frame and contention does not begin until 14335, so an accepted interrupt always lands in the
top border where every stall is zero. **On a 128 the two numbers change and the gap does not
close:**

| | 48K | 128 |
|---|---|---|
| `/INT` asserted from | frame T-state 0 | frame T-state 0 |
| interrupt accepted no later than | ~32 + one instruction | ~32 + one instruction |
| first contended T-state | 14335 | ≈14361 |
| **gap** | **≈14300 T-states** | **≈14300 T-states** |

Both machines raise the frame interrupt at the top of the frame and both begin contending roughly
14300 T-states later. Acceptance cannot be deferred past the window's end — if the line has dropped
the offer is not made — so the acknowledge lands in the top border on **both** machines, for the
same structural reason, and the shadow screen, the second ROM and the per-bank rule are all
irrelevant to it because none of them moves either endpoint.

**The argument does not depend on the disputed number, which is the point of stating it this way.**
Decision 4 says the 128's `FIRST_CONTENDED_T_STATE` is transcribed rather than established. This
conclusion survives that: contention begins roughly sixty display lines into the frame on any
Spectrum, so the figure would have to be **under about forty** for an acknowledge to reach it, and
no candidate value for any machine in this family is remotely near that. A conclusion that holds
across the whole plausible range of an unsettled input is worth more than one that needs the input
settled first — and noticing which kind you have is the difference between this paragraph and the
one in `ula.rs` it corrects.

**So the hand-off is answered in the negative, and that is the finding.** `ula.rs`'s sentence *"the
128 has a different frame geometry"* is true and does not imply what it was written to suggest. It
is the shape `STATUS.md` names: an argument that names real quantities and identifies the wrong
mechanism.

There is no second route through software either, and it is worth closing them all explicitly:
a snapshot cannot help (`Spectrum::step` offers an interrupt only while the ULA asserts one); the
NMI cannot help (this machine has no NMI source); a long instruction cannot help (acceptance is at
instruction end, and the line has dropped); and lengthening `INTERRUPT_T_STATES` cannot help,
because the ungraded uncertainty there is single-digit T-states against a gap of fourteen thousand.

### What **does** decide it, and it has been available since M5

*"No test can currently tell the two models apart"* is true of **software running on the machine**
and false of **a test driving the bus**. `Cpu::new`, `Cpu::interrupt` and `Ula::new` are all public;
`ula.rs`'s own unit tests already position a ULA at a chosen frame phase and synthesise tick
streams by hand. A test that builds a `Cpu<Ula>`, puts the clock inside the contended window, points
`IR` at a contended bank and offers an interrupt separates the two models by a measurable number of
T-states.

That test **pins** the model; it cannot **grade** it. What says which model is right is the hardware
rule, and this repository has already written it down with its reasoning
([`Z80-REFERENCE.md`](Z80-REFERENCE.md)): *"The Z80 asserts `/M1` together with `/IORQ` in place of
`/MREQ`… What the address bus carries is `IR` — the refresh address, not an address to fetch
from."* One machine cycle, therefore one contention point.

Note what is **not** in dispute: the acknowledge's *length*. `crates/z80` charges 7 T-states for a
maskable acknowledge and 5 for an NMI, and those totals are inside the FUSE-graded envelope. The
only question is whether those 7 T-states are one cycle or seven, and the hardware rule answers it.

### The fix, and why it is not the guess it was called

```rust
/// The acknowledge cycle of an accepted interrupt: one machine cycle, `t_states` long,
/// with the refresh address on the bus and no transfer. Defaulted, so implementing it
/// is optional and adding it breaks nothing.
fn acknowledge(&mut self, addr: u16, t_states: u8) {}
```

This is the `fetch` precedent exactly — a seventh defaulted method on `Bus`, non-breaking by
construction, which `ARCHITECTURE.md` records was *checked rather than asserted* when `fetch`
landed. `Ula` implements it as the contention charge plus arming `covered_t_states`, which is what
every other cycle already does.

**The length travels with the call, and that is deliberate.** `crates/z80` keeps
`INTERRUPT_ACKNOWLEDGE` and `NMI_ACKNOWLEDGE` private today with a stated reason: *"An acknowledge
has no callback… exporting its length would hand out a number a `Bus` cannot act on."* A callback
falsifies the premise, not the rule — so rather than exporting two constants, the callback carries
the figure and the constants stay private. The reasoning that justified hiding them is preserved in
a stronger form.

**Ruling: fix it at M7.** The argument for leaving it was *"a fix would be an unverifiable guess"*,
and on inspection it is not a guess in the sense that phrase usually carries. It is not a
*magnitude* being invented; it is a *structural* question — one cycle or seven — that a documented
hardware rule already answers, and the current model knowingly contradicts a rule this repository
states in its own reference. Leaving a known contradiction in place because nothing can see it is
the mirror image of `MACHINE.md`'s standing lesson about *"reporting the absence of a distinguishing
test as evidence of correctness."*

**And the fix closes a model defect while closing no verification gap. Both halves go in the
register.** Nothing grades the acknowledge against hardware, nothing in this milestone will, and —
newly, and this is the part worth carrying forward — **nothing can, because no software on either
machine can reach the case.** That is a stronger and more useful statement than "it is currently
ungraded", and it converts an open row into a closed question with a permanent asterisk.

---

## Decision 6 — the AY-3-8912, and the only honest answer to "what grades sound" — **ruling**

This is the hardest verification problem in the milestone and it deserves the plainest statement.
**A wrong tone is audible to a human and invisible to every test this repository can write.** There
is no oracle for sound, there is no round trip, and there is no equivalent of "a demo tears or it
does not" because a listener cannot tell a 2 % pitch error from a correct one.

The way through is not a better ear. It is to notice that **the AY is not a sound problem. It is a
state-machine problem that happens to drive a speaker**, and a state machine is exactly what this
project can grade.

### What is actually checkable, and at what tier

| Property | Tier | Why |
|---|---|---|
| **Register write masks** — several registers are narrower than 8 bits and the excess is dropped | **derived** | Transcribed from the datasheet. Grading against the transcription cannot discover the transcription is wrong — but software *reads registers back*, so a wrong mask is a wrong value returned to a guest |
| **Port decode** — `0xFFFD` select/read, `0xBFFD` write, and the address lines actually decoded | **derived** | Same class as the `0x7FFD` decode. Verify against a source; the *shape* (two `if`s, not a `match`) is forced by the overlap argument in Decision 2 |
| **Noise LFSR period** | **proven**, and its strength is measured below | Run the register and count until it repeats. **This needs no external source at all** |
| **Envelope shape aliasing** — of the 16 four-bit shape values only 8 are distinct behaviours; the lower eight collapse into two blocks of four | **proven** | A structural property of the CONT/ATT/ALT/HOLD decode, checkable as an equivalence over all 16 without knowing what any shape *sounds* like. **Which two upper shapes they alias to is a transcription** — the structure is provable, the targets are not |
| **Counter periodicity** — a tone register value of *n* produces a square wave of a period that is an exact function of *n* | **proven** | Pure arithmetic over the state machine, exhaustively checkable over all 4096 tone-period values |
| **Mixer polarity** — the enable bits are **active low** | **proven**, once the convention is fixed | Inverting it is a classic emulator defect. With every channel disabled the output is silent; the test is a boolean, not a magnitude |
| **The volume table** — 16 levels, roughly logarithmic | **derived**, and the magnitudes are unverifiable here | Nothing this project can build distinguishes a right table from a plausible one |
| **That it sounds correct** | **observed**, T4, and not automated | A human listens to a 128 tune |

**The register file has fifteen registers, not sixteen, and that asymmetry is a trap for the
snapshot.** The `-8912` is a cut-down `-8910` with **one** 8-bit I/O port rather than two — the 128
wires it to an external keypad almost nobody owned — so `R15` does not exist. The World of Spectrum
128K reference states the range directly:

> *"OUT (0xfffd) - Select a register 0-14"*, *"IN (0xfffd) - Read the value of the selected
> register"*, *"OUT (0xbffd) - Write to the selected register"*.

But **`.z80` v3 reserves sixteen bytes at offsets 39–54 regardless.** So the format has a slot the
machine has no register for, and whatever the model puts there **round-trips perfectly and is
invisible to every round trip** — the exact defect shape `M6.md` Decision 7 is about, arriving in a
new field. Decide what `R15` holds and what a read of it returns, write the decision down, and gate
it with a transcribed vector rather than a round trip, because a round trip cannot see it.

~~**Not confirmed, and it should be:** which address lines actually decode the AY ports. The
addresses `0xFFFD` and `0xBFFD` are well attested; the decode rule behind them — the `A15`/`A14`/`A1`
pattern assumed in Decision 2's overlap table — **was searched for and no source stating it was
found.** It is consistent with those two addresses and with the `0x7FFD` decode's style, and that
is inference, not a citation. Treat the overlap table's AY rows as the least-supported claim in
this document.~~

> **Confirmed 2026-09-01, and this document's self-declared weakest claim is now its
> best-sourced one.** The Sinclair *Servicing Manual* §5.6.3 states the decode from the
> circuit, exactly as §4.12.11 does for `0x7FFD` — read off the rendered page rather than an
> OCR layer:
>
> > *"PSG is decoded in IC29 from I̅O̅R̅Q̅ with R̅D̅ or WR (I/O read/write cycle) and ZA1 = 0 and
> > ZA 15 = 1 (address FFFD_H with A14 high; address BFFD_H with A14 low)."*
>
> **And the shape it gives is not the shape this document assumed.** The manual's own equations
> are two stages, not one three-bit pattern:
>
> ```text
>   PSG  = IORQ · (RD + WR) · (ZA1 = 0) · (ZA15 = 1)   <- only A15 and A1 select the chip
>   BDIR = PSG · /RD ,  BC1 = PSG · A14                <- A14 steers; it does not gate
> ```
>
> Two consequences fall out of the equations and appear in **no table in the manual**: at
> A15 = 1, A1 = 0 the AY is *always* engaged — there is no no-match state — and a **read** of
> the `0xBFFD` family gives `BDIR = 0, BC1 = 0`, which is INACTIVE, so it floats. **`A0` is
> not in the AY's decode at all.** `crates/spectrum/src/ula.rs` encodes the two stages rather
> than the pattern; the machine's *behaviour* under the old encoding was already right, and
> what changed is that it now says why.
>
> One correction rides along: §4.12.11 reads **"IORQ and R̅D̅/WR active low"** — RD *or* WR —
> where `ula.rs` had transcribed *"W/WR"*. The image beats the OCR, and the read-or-write
> reading is what the crash-on-read behaviour of `0x7FFD` independently predicts.

**The dividing line runs between structure and magnitude.** Every *structural* property above is
provable in-repo with no corpus; every *magnitude* is a transcription that nothing here can
adjudicate. M7 should grade the first list exhaustively and state the second as untested, and it
must not blur them. Blurring them is how a gate comes to grade less than it appears to, which
`STATUS.md` records three times already — as gates that *nothing ran*, under *A gate that nothing
runs, for the third time*, which is a narrower family than this sentence's wording — and which
`MACHINE.md:132`, not `STATUS.md`, calls *"the worst form so far"* (checked against both files on
2026-09-01; `STATUS.md` contains the word *worst* nowhere) — and
an AY gate is an unusually easy place to do it, because a suite of green sound tests reads as
"the sound chip works" whatever it actually asserted.

### What the LFSR period test is worth, measured rather than asserted

The noise generator is reported to be a 17-bit LFSR with taps at bits 0 and 3. A period test needs
no source for that: run the register from a non-zero seed and count until it repeats. But *how much*
it is worth is a question with an exact answer, and this project's habit is to take it rather than
gesture at it. Sweeping all sixteen tap pairs `(0, t)` for a 17-bit Fibonacci LFSR and measuring
each period:

| Period | Tap pairs `(0, t)` |
|---|---|
| **131071 — maximal** | t = **3**, 5, 6, 11, 12, 14 |
| everything else (273, 1023, 4599, 35805, 114681) | t = 1, 2, 4, 7, 8, 9, 10, 13, 15, 16 |

**So a green period test eliminates 10 of the 16 candidate tap positions and cannot distinguish
the remaining 6 from each other.** That is a real gate with an exactly enumerated blind spot, which
is a different thing from either "the noise generator is tested" or "there is no test". It is worth
having, it costs one loop, and it must be described as what it is — the temptation to let a gate
that kills five-eighths of the error space stand in for one that pins the answer is the same move
`STATUS.md` catalogues as letting a good account of the majority stand in for the whole.

What would pin it: a published measured trace from real hardware, or software whose output audibly
depends on the noise sequence rather than merely on its statistics. Neither is in reach here.

### The output is a test instrument, not a product

**M7 models the AY's state machine and produces a deterministic sample stream. It does not open an
audio device.** The audio *device*, the mixing down to one stream, and the resampling to a host
rate are frontend concerns and belong in the milestone that owns the frontend. **What the machine
emits is this crate's; what a speaker does with it is M8's.**

> **This paragraph said the beeper was M8's, per `M6.md`, and the ruling has gone the other
> way — the beeper is M7's.** The superseded sentence read: *"`M6.md` assigns the beeper — bit 4
> of a `0xFE` write — to M8, and M7 does not overturn that."* It is overturned now, and the
> reason is the same one that puts the AY here: **the beeper is a ULA feature and the AY is a
> 128 device, so both are parts of the machine, and `crates/spectrum` is where the machine is
> modelled.** `ula.rs`'s `out_port` already decodes that byte for the border; the speaker bit is
> the same decode, in the same function, on the same write.
>
> **The constraint below gets easier rather than harder.** It asks that the AY's hash cover the
> AY's own output and not a mix, precisely so that M8 adding the beeper could not falsify an M7
> gate. With the beeper in M7 that is now a rule about two sources **inside one milestone**,
> where it can be held by construction instead of by a note to a future reader — and it is still
> required, because the two sources must stay separable for exactly the same reason.

What the sample stream buys is the one instrument that works on an output with no oracle:
`MACHINE.md`'s verification item 4, **frame-hash regression** — *"does not prove correctness —
proves change, which is what catches a regression once something works."* That is precisely the
right strength of claim for the AY, and naming it as such is the whole point.

**One design consequence follows and must be built in from the start: the hash must be over the
AY's own output, not over "the machine's audio."** If it covered a mix, **the beeper landing**
would change every hash and falsify an M7 gate that was never wrong — a gate that goes red for a
reason unrelated to what it grades is a gate that gets muted, which `ARCHITECTURE.md` gives as the
reason its timing rows are refused rather than asserted.

> **This said *"M8's addition of the beeper"*, and the beeper is now M7's.** The hazard is
> unchanged and the deadline moved closer: it used to be a trap set for a later milestone, and it
> is now a trap set for **this one**, which is strictly better because the person who can spring
> it is the person reading this. Two sources, two hashes, mixed nowhere in `crates/spectrum`.

### What nothing grades, stated in the same breath

- **Any magnitude.** Volume levels, the exact envelope period divisor, the tone prescaler's
  constant. Transcribed, and nothing here can adjudicate a transcription.
- **The mapping from register values to a pitch a human recognises.** No test in this repository
  distinguishes correct from consistently-wrong-by-a-factor.
- **Timing of AY writes relative to the frame.** Music drivers write the AY from the interrupt
  handler; the *audible* result depends on when in the frame the writes land, and nothing grades
  that.
- **Whether it sounds right.** T4, manual, and the honest bottom of this table.

---

## Decision 7 — `.z80` version 3 already carries everything M7 needs, so still no `.szx` — **ruling, and it overturns `M6.md`**

`M6.md` Decision 9 deferred `.szx` with a forward-looking reason: *"Its extensibility pays off at
**M7**, where the AY registers, the paging port and the second ROM all need somewhere to live and
`.z80` v3 carries them awkwardly."*

**That guess does not survive contact with the format, and the reason is already in this
repository.** `crates/spectrum/src/snapshot/z80.rs` reads the additional header field by field and
names them:

```rust
let _paging = extra.u8()?;      // 35: last OUT to 0x7FFD, on a 128
let _ay_selected = extra.u8()?; // 38: last OUT to 0xFFFD
extra.skip(AY_REGISTER_COUNT)?; // 39-54: the sound chip's registers
```

Those are named fields at fixed offsets, and `AY_REGISTER_COUNT` is already a constant in the file.
Pages 3–10 already map to banks 0–7 — verified against the format description by M6's implementer.
**`.z80` v3 carries the paging byte, the AY select latch, all sixteen AY registers and all eight
RAM banks, in dedicated fields.** There is nothing awkward about it. What `.szx` would add is
`MEMPTR` — which `M6.md` itself notes *"buys nothing measurable today: nothing in this project can
grade `wz`"* — and extensibility for requirements that do not exist.

**Ruling: no `.szx` at M7.** M6's reason for deferring it to M7 is retired rather than inherited,
which is the correction being made in the open. The three-parsers-tripling-the-hostile-surface
argument stands unchanged.

### What `.z80` support at M7 actually requires

- **`Model` gains its second variant and `Snapshot` gains a `model` field.** `M6.md` predicted
  exactly this — *"at M7 those rejections become support, and that is where a `Model` enum earns
  its second variant. A one-variant enum today would be scaffolding"* — and the prediction holds.
- **The hardware-mode check becomes version-dependent in both directions.** M6 found that v2 and v3
  *renumber* the modes. `Version::is_48k` becomes `Version::model(mode) -> Option<Model>` and the
  existing test `the_versions_disagree_about_hardware_mode_three` becomes the anchor for a table
  over both versions and both models. **This is the trap M6 flagged and it is the highest-risk line
  in the parser**: accepting a mode unconditionally *"would load a 128 snapshot as a 48K and
  silently lose five banks."*

  Half of the table is already established in this repository and half is not, and the difference
  should be respected rather than smoothed over. **`z80.rs` already asserts that mode 3 is a 128K
  in v2 and a 48K + M.G.T. in v3**, checked against the format description by M6's implementer — so
  the v2 side of the 128 row is transcribed and verified. **The v3 128K mode number is not**, and
  the current code only knows it is *not* 48K. Transcribe it; do not infer it from the v2 value,
  because renumbering is the whole hazard.
- **`Spectrum::restore` becomes fallible, and the rule is symmetric.** A 128 snapshot restored into
  a 48K has five banks with nowhere to go; dropping them silently is the *"silent last-write-wins"*
  M6 rejected for duplicate pages. The other direction is broken too and less obviously: a 48K
  snapshot restored into a 128 leaves 48K code running against the **128 editor ROM**, because a
  48K file carries no paging byte to select the 48 BASIC ROM. So the check is `snapshot.model ==
  machine.model`, both ways. `restore(&Snapshot) -> Result<(), ModelMismatch>` — the one breaking
  change in this milestone.

  > A 128 *can* legitimately host a 48K image: that is what the ROM's own **48 BASIC** menu entry
  > does, and it is one paging value (48 BASIC ROM selected, paging locked). Making `restore` do it
  > automatically is a plausible convenience and it is **not** built — it is a second meaning for
  > one method, it needs a rule for what the un-restored five banks contain, and no caller wants
  > it. Named here so that if someone later wants it, they add a separate entry point rather than
  > weakening this one.
- **M6's bank-set guard must be redesigned, and it is easy to miss.** `M6.md` closes the
  dropped-bank seam with a test comparing *"the parser's bank set against the set the slot map
  exposes."* **On a 128 that premise dissolves**: banks legitimately exist outside the slot map —
  that is what makes `Memory::bank` unavoidable. The replacement compares the parser's bank set
  against the set the **model** has, not the set currently addressable.

---

## Decision 8 — the gate, and what "128-only software runs" would add that it cannot

### Whether a 128 timing oracle exists — the test to apply, before the answer

`MACHINE.md`'s timing-oracle section closes with *"Anything about a 128. The site states that 128K
and +2 (Grey) editions of these tests exist. Neither was fetched or verified here; M7 should."*

**Before reporting what the survey found, it is worth fixing what would count**, because the
temptation with an artefact like this is to accept "a 128 timing suite exists" as the answer to
"does a 128 oracle exist", and those are different questions. What moved the 48K suite from
*derived* to **measured** was three things, and they are not equally strong:

1. the text attributes its expected values to hardware — *"we ran the tests on real Spectrums"*;
2. its results database is hardware-only by policy — *"Only submit results from genuine hardware no
   emulators!"*;
3. **decisively: the file carries two expected-result tables, and 25 of 28 submitted real machines
   from 9 independent people sort cleanly into exactly those two classes.**

Only (3) is evidence that does not require believing the author. (1) and (2) are the author's own
statements about the author's own process. A table fitted to one emulator has no reason to predict
a second class of machine, and no reason for twenty-five real machines to fall into the two.

> **And here is the asymmetry that decides how much to expect, whatever the survey turns up: (3)
> may be structurally unavailable for a 128, through nobody's fault.** The 48K's two tables exist
> because real 48Ks genuinely come in two contention classes — the early/late split — so the suite
> *had* to carry two, and the sorting of real machines into them is a prediction that could have
> failed and did not. **If 128s are uniform in this respect, a 128 suite carries one table, and the
> single strongest anti-circularity argument cannot be made about it at all.** Not because its
> author was careless, but because the evidence available has a different shape.
>
> The practical consequence: **a 128 suite can be entirely honest and still only reach *derived*
> in this project's vocabulary, unless it has an independent hardware results database.** That
> possibility must be admitted in advance, so that finding one table is read as what it is rather
> than as a defect in the artefact or as grounds for accepting it anyway.

So the survey asks, in order: **(a)** does a self-checking 128 edition exist and can it be fetched;
**(b)** is there a results database of *genuine hardware* submissions, from more than one person;
**(c)** if there is one table rather than two, is there anything else that rules out an emulator
origin? Failing (b) and (c) together means the artefact is a second implementation's opinion — which
is worth more than our own arithmetic and much less than the 48K oracle, and must be labelled
accordingly rather than promoted by association with its 48K sibling.

### The survey, taken 2026-09-01

**Result: no usable 128 oracle is in hand, and the shortfall is on the decisive leg rather than on
availability.** Candidates, in the same shape as `MACHINE.md`'s 48K table:

| Candidate | What it is | Obtainable? | Hardware results database | Tier |
|---|---|---|---|---|
| **`timing_tests-128k_v1.0.z80`** — the ZXSpectrum4.net 128K edition, by the same author as the 48K suite this project already uses | **Fetched, 2026-09-01 — it is now in `testdata/timing/`; see *Step 0, taken*, below, which answers the table-count question this row's tier depended on.** **Yes, and a live mirror was found.** Not at the origin: `zxspectrum4.net/downloads/timing_tests/` **302-redirects to the site root**, so the whole download directory is gone — worse than the 404 this document's 48K section records. Not in the [MrKWatkins mirror](https://github.com/MrKWatkins/EmulatorTestSuites) either; its `Timing/` directory was enumerated through the GitHub API and contains exactly one `.z80`, the 48K file. **It is on [SoftSpectrum 48's test-results page](https://softspectrum48.weebly.com/test-results.html)**, at `https://softspectrum48.weebly.com/uploads/6/6/7/5/66753101/timing_tests-128k_v1.0.z80` | **None.** `spectrum128k_timing_results.htm` returns **404** — probed, not inferred — and neither `op_timing.php` nor the site's front page links to any 128 equivalent. The only results page is [`spectrum48k_timing_results.htm`](https://www.zxspectrum4.net/downloads/spectrum48k_timing_results.htm) | **derived at best**, and it cannot climb higher on present evidence — see below |
| Patrik Rak's **`ptime`** and Weiv's **`Ptime-128`** | 128K **screen-switching** timing tests — the shadow-screen bit's own timing, which nothing else here would grade | Listed on the SoftSpectrum 48 page | Unknown | **Not investigated.** The highest-value follow-up after the suite itself, because they test a property that is 128-only by construction |
| *ULA 128 Timing Test*, *IR Contention 128* | Catalogued by [redcode](https://github.com/redcode/ZXSpectrum/wiki/Tests) as 128 contention tests | Not fetched | — | Redcode's list *"does not specify whether these tests are self-checking"*, and their 48K siblings are classified **observed** in `MACHINE.md` — mostly on-screen patterns, several needing a floating bus this machine does not model |
| [World of Spectrum, *128K Technical Information*](https://worldofspectrum.org/faq/reference/128kreference.htm) | The community reference document | Yes | n/a — it is prose | **derived** |
| [Sinclair Wiki, *Contended memory*](https://sinclair.wiki.zxnet.co.uk/wiki/Contended_memory) | The community's contention page | Yes | n/a | **derived**, and `MACHINE.md` already classifies it as *"this project's own source. Grading against it would be circular by construction"* |

**Two things follow, and the second matters more than the first.**

**1. The artefact exists and can be had — so M7 should fetch it and run it.** It would be a genuine
instrument: self-checking, running on the real machine, with expected numbers this project did not
write. That is strictly more than we have now and the fetch costs minutes.

**2. It cannot reach *measured*, and that is not fixable by looking harder.** The three legs that
carried the 48K suite were the author's hardware attribution, a hardware-only submission policy, and
— decisively — 25 of 28 real machines from 9 independent people sorting into the file's two expected
classes. **For the 128 the third leg has no counterpart at all** (there is no results page: 404,
probed) and the second applies to a database that does not exist. The two hardware-policy quotes on
`op_timing.php` are **site-wide and not model-scoped**; they do not establish that any 128 was ever
submitted.

What is left is the author's own statement about his own process — which is exactly what the 48K's
third leg exists so that nobody has to rely on. So running the 128 suite green would say *"this
emulator agrees with an artefact of unverified provenance"*, and running it red would be a real
finding. **An asymmetric instrument: its failures are informative and its successes are not.** That
is worth having and it must be labelled that way, because a green run reads as vindication whatever
the header says.

**There is weak evidence it carries only one table, which would settle the question structurally.**
SoftSpectrum 48's own note is that the 128 file *"must be run with late timings in 48K mode"* — a
single named timing mode, with none of the early/late auto-detection the 48K suite advertises. That
fits the prediction above: real 48Ks split into two contention classes and 128s do not, so a 128
suite has no second class to predict and cannot make the argument that rescued its sibling.
**Confirming the table count is the single most decisive thing anyone can do here**, it needs only
the downloaded file, and it is implementation step 0 below.

> **One thing that would partially rescue it, recorded because it is unverified and worth chasing.**
> A report surfaced in search that Fuse fails 128K tests 4, 17, 18, 26 and 33, and that JSpeccy and
> the MiSTer core fail those plus two more. **If true, the tables disagree with at least three
> independent emulators, which is hard to explain if they were fitted to one.** That would be real
> anti-circularity evidence of a different kind from the 48K's. The source thread returned HTTP 403
> and **this was not verified** — it is a lead, not a finding, and it is written here as a lead.

### Step 0, taken 2026-09-01 — the file is in hand, and the question is answered

The paragraph above says confirming the table count *"is the single most decisive thing anyone can
do here"*. It was done. **The prediction is confirmed, and the artefact is in one respect worse
than the prediction allowed for.**

#### The corpus record

Fetched from the SoftSpectrum 48 mirror, which served it at the URL this document located
(HTTP 200, 12960 bytes, no redirect — the request was made with `-L` and the effective URL came
back unchanged). It lives beside the 48K suite under `testdata/timing/` and is **gitignored like
every other fetched corpus**: `git check-ignore -v` names `.gitignore:3`, `testdata/**`, verified
rather than assumed.

> **What is *not* yet true, said here rather than discovered later.** The absence policy in
> `crates/testsupport` is **not applied to this file, because no gate reads it.** There is nothing
> to route until step 4 builds one. Writing *"absence is routed through `crates/testsupport`"* today
> would be prose asserting a protection no code provides — which `testdata/README.md` records
> itself doing once, for the committed ROM, with the result that deleting `48.rom` left the suite
> green and the test count unchanged. **When step 4 reads this file it must go through
> `corpus_file` / `skip_absent_corpus` exactly as the 48K suite does, with no new convention**, and
> that sentence becomes true at the moment the code makes it true and not before.

```sh
mkdir -p testdata/timing
curl -fSL -o testdata/timing/timing_tests-128k_v1.0.z80 \
  https://softspectrum48.weebly.com/uploads/6/6/7/5/66753101/timing_tests-128k_v1.0.z80
shasum -a 256 testdata/timing/timing_tests-128k_v1.0.z80
```

| | |
|---|---|
| Size | 12960 bytes |
| SHA-256 | `fedc228ddef76cefb7b81dd6e18600cca2fd826fc18b4bc3f773cfdf2e7fffc4` |
| SHA-1 | `9b1b4112510f65cb3368f41aa383e1d43a383b50` |
| Fetched | 2026-09-01, `https://softspectrum48.weebly.com/uploads/6/6/7/5/66753101/timing_tests-128k_v1.0.z80` |
| Author | Richard Butler, ZXSpectrum4.net — the same author as the 48K suite. The snapshot's own BASIC still prints `to - richard@ZXSpectrum4.net` |
| Licence | **No licence text in the file, and none on the page that serves it** — checked. Same footing as the 48K suite, of which `testdata/README.md` says *"Fetched, gitignored, and not redistributed here, like every corpus except the ROM… the suite itself is Richard Butler's."* **That is exactly why both are fetched rather than committed**, and the 128 file changes nothing about the policy — one more reason the `.gitignore` question in Decision 9 is about `testdata/roms/` alone |
| Structure | `.z80` **version 3** (additional header 54), hardware mode **4** (128K), PC `0x15E8`, all **eight** RAM banks present as pages 3–10 |

**The header carries one fact worth reading before anything else: the last `OUT` to `0x7FFD` is
`0x30`.** Bits 4 and 5 set — **48 BASIC ROM selected and paging locked**, bank 0 at `0xC000`,
screen bank 5. That is the machine's own statement of the SoftSpectrum note *"must be run in 48K
mode"*, and the BASIC front end says it twice on screen (`** MUST RUN IN 48k MODE **`). So the
suite exercises the **128's frame geometry** through a **48K memory map**: it grades timing, and it
grades nothing about paging, the shadow screen or the second ROM. Nothing in M7's Decision 1–3
is touched by it.

#### One table. The second one is a 48K leftover, and that is a trap.

**The 128 file carries one live expected-result table.** Four independent lines of evidence, each
taken from the file's own bytes with a decoder written for this check rather than through
`spectrum::snapshot`:

1. **The Early/Late classification block is deleted from the program.** The 48K's BASIC has lines
   **805, 807 and 850** — `TYPE1 (Early) timings detected` / `POKE 40004,0`, `TYPE2 (Late)` /
   `POKE 40004,1`, and `UNKNOWN TIMING!`. The 128's BASIC has **none of them**: its line numbers
   run `803, 900` where the 48K's run `803, 805, 807, 850, 900`, and the byte strings `TYPE1`,
   `TYPE2`, `UNKNOWN TIMING` and `Early/Late` occur **zero times** anywhere between `PROG`
   (`0x5CCB`) and `VARS` (`0x7210`).
2. **So the selector can never fire.** Both files still carry the same line 5060 —
   `LET k=57856+(a*10)+(c*5): IF (PEEK 40004)=1 THEN LET k=k+512` — but nothing in the 128 program
   writes 40004, and the byte at 40004 in the snapshot is `0`. `57856` is `0xE200`; `+512` is
   `0xE400`. **The 128 suite reads `0xE200` and only `0xE200`.**
3. **The `0xE400` table is the 48K's `TYPE2 (Late)` table, byte for byte.** All **512** bytes are
   identical between the two files, and all **73** populated rows agree. By contrast the `0xE200`
   tables agree on **0 of 73** rows. It is an un-updated inheritance from the file it was edited
   out of, not a second class of 128.
4. **The deleted lines' bytes are still lying above `VARS`.** Three copies of
   `TYPE1 (Early) timings detected.` and one `t$="Early/Late Timing"` survive at `0x7424`,
   `0x7473`, `0x7511` and `0x759D` — above the program, in memory the BASIC no longer owns. That
   is the signature of the 128 edition being **produced by editing the 48K program in place**:
   delete the detection lines, overwrite the first table, adjust the code. Measured: over
   `0xC000..0xE1FF` the machine code differs in **254 of 8704 bytes**, in **ten** runs, clustered
   into `0xDDE6..0xDEDC` — where the `IN`/`OUT` groups were rewritten to different ports — plus
   four small edits below `0xD010`. Everything else is the 48K program unchanged.

> **The trap, stated plainly because it is a live one and it is cheap to fall into.** A future
> implementer extending `timing_oracle.rs` to the 128 will find `EARLY_TABLE = 0xE200` and
> `LATE_TABLE_OFFSET = 512` already written, and both addresses populated in the 128 file with
> plausible-looking numbers. **Reading `0xE400` on the 128 file compares a 128 against 48K
> hardware.** It would be red, it would be red for a reason that has nothing to do with the 128,
> and the numbers are near enough to look like a small modelling error rather than a category
> error. The 128 file has one table and its address is `0xE200`.

**So the structural prediction this document made in advance is confirmed: leg (3) — the
decisive one — is unavailable for the 128, and not through the author's fault.** There is no
second class to predict because the file does not classify, and there is no hardware results
database to sort machines into classes (`spectrum128k_timing_results.htm` re-probed here on
2026-09-01: **404**, against **200** for the 48K's).
**The artefact is capped at *derived*. Its failures are informative; its successes are not.** That
framing is unchanged and this evidence is what earns it rather than assumes it.

It is also fair to record the cost of finding out: the leftover table means *"the file was
carefully maintained"* cannot be assumed of it, which is a small independent reason to keep its
green cheap and its red expensive — exactly the asymmetry already chosen.

#### What the file says about **14361**: nothing directly, and here is why that is not a shrug

**No literal `14361` appears anywhere in it**, and none could: the suite's measured quantity is a
count of loop iterations completed in one frame, integrated over hundreds of iterations. Shifting
*where* the contended window opens by a few T-states moves almost none of that integral, because
the same 192 display lines are contended either way. The file can **refute** a 128 contention model
— offset, pattern and I/O rule graded jointly — the moment one exists. It cannot **yield** the
offset by arithmetic, here or ever.

**What the file does settle arithmetically is the frame length, and it does it from its own
bytes.** The suite's detection group is `JP (HL)` jumping to itself in uncontended memory — four
T-states and one `R` increment per iteration — and `R` is seven bits. The extra
`70908 − 69888 = 1020` T-states are `255` iterations, and `255 ≡ −1 (mod 128)`. So a 128 running
the same program from the same start should report the 48K's reading **minus one**:

| | detection `R`, uncontended | source |
|---|---|---|
| 48K `TYPE1 (Early)`, `0xE200` | **2** | read out of the file; BASIC line 805 tests `x=2` |
| 48K `TYPE2 (Late)`, `0xE400` | **122** | read out of the file; BASIC line 807 tests `x=122` |
| 128, `0xE200` | **121** | read out of the file |

`122 − 1 = 121`. `2 − 1 = 1`, which is not 121. **The 128 table's own detection row is consistent
with the 48K `Late` reading carried across a 70908-T-state frame, and inconsistent with the
`Early` one.**

That was derived from the bytes before the note was read, and the note then agrees with it. Fetched
and quoted here rather than inherited — `https://softspectrum48.weebly.com/test-results.html`,
HTTP 200, 2026-09-01:

> *"Richard Butler's timing tests 48K and 128K (the latter must be run with late timings in 48K
> mode)."*

Both halves of that parenthesis are now independently confirmed from inside the file: *48K mode* by
the header's `0x7FFD = 0x30`, and *late timings* by the detection row's 121. **Two independent
confirmations of a note is a better position than the note alone**, and it is the only place in
this milestone's survey where that has been available.

*(Also re-probed rather than quoted, same date:
`https://www.zxspectrum4.net/downloads/spectrum128k_timing_results.htm` → **404**, while the 48K's
`spectrum48k_timing_results.htm` → **200**. The missing results database is still missing, and
that is what the *derived* cap rests on.)*

**Its blind spot, measured rather than waved at.** The check is `(F − 69888)/4 ≡ −1 (mod 128)`, so
it is periodic in 512 T-states: it separates 70908 from 69888, and it does **not** separate 70908
from 70396, 71420, 71932 or 72444. So it is a genuine independent corroboration of 70908 — a
fourth lineage, and the first that is not a document — with a stated 512-T-state granularity. It
adds **nothing** to 14361, which remains exactly where Decision 4 leaves it: one ancestor seen
twice.

> **And it makes one concrete prediction that must not be misread as a defect.** This emulator
> reproduces `TYPE1 (Early)` — `timing_oracle.rs` asserts that as a pinned fact. If a 128 model
> here inherits that interrupt phase, **its detection reading will not be 121 and the 128 suite
> will go red on every row**, for a reason that is about the early/late class and not about the
> 128's contention at all. Whoever runs it first should take the detection row before taking any
> other row, and should say which class the disagreement is about before touching a constant.
>
> The mechanism behind the 48K tables' 120-`R` gap on an *uncontended* group is **not established
> here** and should not be guessed at: `timing_oracle.rs` records that moving the interrupt and
> the window together leaves the detection at `TYPE1`, so a one-T-state window shift is plainly
> not what separates 2 from 122. Whoever needs it should derive it from the detection group's own
> code, which is in the file.

#### The mechanism, established 2026-09-01 — and it moves the 128 prediction onto one field

The paragraph above says the mechanism is not established and should be derived from the group's
own code. **That was done, and it is established.** The derivation and its measurements live in
`crates/spectrum/tests/timing_oracle.rs`'s module documentation, where the code is; what belongs
here is only what it changes about this milestone. In one sentence: **`R` counts four-T-state
`JP (HL)` iterations, and a `TYPE2` machine fits eight fewer of them because it takes one extra,
*nested* interrupt during the suite's own synchronisation — worth exactly 32 T-states.** 120 is
−8 wrapped into seven bits, and it was never 120 of anything.

**The amplifier, because it is the part that makes a one-T-state difference visible.** The suite
installs a nine-byte handler whose first three instructions are `INC C` / `EI` / `RET C`. `EI`
does not re-enable interrupts until after the instruction that follows it, so the first moment the
CPU can see `/INT` again is 19 (the IM2 response) + 4 + 4 + 5 = **exactly 32 T-states** after it
accepted the previous one — exactly the far edge of the 48K's 32-T-state `/INT` window. Whether
that tie resolves as "still asserted" is the whole early/late split. If it does, a second interrupt
is taken and costs 19 + 13 = 32 T-states before the same fall-through; the measurement loop starts
32 T-states later; eight fewer iterations fit. `INC C` and `RET C` do nothing else — the carry is
clear, so `RET C` never returns — and they are the two instructions that make 13 of the 32.

**Measured, not only derived.** Mutating `Timing::SPECTRUM_48K`'s `interrupt_t_states` in a scratch
clone, contention untouched at 14335: at **32** the detection row is bit-exact `TYPE1` and 0 of 68
rows disagree; at **33** it is bit-exact `TYPE2` and **65 of 68** rows follow it onto the `TYPE2`
table. Sweeping 1–65 pins the constant to **`17..=32`** — below 17 contended rows start
disagreeing, above 32 the class flips, and from **44** the suite stops terminating altogether
because the *other* handler nests as well (its own first sample falls at 43; the derivation
predicted the 43/44 edge before the sweep was run).

**Three things follow for M7, in descending order of how much they change.**

1. **`SPECTRUM_128`'s `interrupt_t_states` is now the field that decides the 128 suite's first
   run, and its doc comment is out of date.** It currently reads *"inherited from the 48K and
   measures nothing"* — and the 48K half of its supporting argument, *"the 48K's own 32 is
   ungraded"*, is refuted by the sweep above. Applying the same closed form to a 70908-T-state
   frame: entry at 292 gives `R` = **1**; entry at 292 + 32 gives `R` = **121**, which is what the
   128 file carries. So the inherited 32 predicts **1**, a detection row that matches nothing and
   a red row everywhere; **any value in `33..=43` predicts 121**. That band is *derived* — the
   instruction sequences it rests on are byte-identical between the two files, verified by diff,
   but there is no 128 here to run. **It is a prediction to test, not a constant to adopt**, and
   changing it before the 128 file has been run would be fitting the model to the answer.
2. **Decision 4's design is vindicated on a point it did not know it was making.** `Timing`
   carrying `interrupt_t_states` as a field *"precisely so that a different 128 value is a data
   change rather than a code change"* was written when the field looked inert. It is not inert: it
   is the single highest-leverage number in the 128's timing, and 14361 — the figure this document
   spends most of its worry on — is *not* what the 128 suite's detection row is sensitive to.
   **The suite cannot see the contention offset at all on that row**, which is exactly what the
   earlier mutation run reported and what made the 120 look inexplicable.
3. **The early/late split is a property of the interrupt, not of contention.** Running the 68 rows
   with the window at 33 *and* contention moved to 14336 scores **worse** (4 disagreements) than
   with contention left at 14335 (3). So a `TYPE2` machine is not "a `TYPE1` machine with the
   contention window one T-state over"; the axis the suite detects is the interrupt window, and
   the two should stop being spoken of as one thing.

**And one thing that is not established, said plainly.** Three of the 68 rows still disagree with
the `TYPE2` table at window 33 — `group 3 contended`, `group 7 uncontended`, `group 34
uncontended` — so a `TYPE2` machine is not exactly this machine with a 33-T-state window. Group 7
locates the residue: its body ends `DI` / `EI` / `JR`, and the arithmetic puts an instruction
boundary **exactly on** the frame's interrupt T-state, where this machine takes the interrupt and
the hardware `TYPE2` machine did not. What would settle it is a `TYPE2` hardware submission's full
73-row result set, or a sub-T-state interrupt-sampling model. Neither is in hand; neither is needed
for the detection row, which is bit-exact.

#### Which groups could ever apply, and which could not

The 128 file's group layout is the 48K's: **38 populated group slots, 0–37, holding 73 rows** —
group 0 is the detection group and has an uncontended row only, groups 36–37 have a contended row
only, and the other 35 have both. That is the same shape and the same count as the 48K file, which
is what makes the two comparable at all. `timing_oracle.rs` grades 1–34 and excludes 35–37
**by name** because they read the floating bus. That exclusion transfers
unchanged and gets worse rather than better: the 128's floating bus differs from the 48K's, and
this machine models neither (`FLOATING_BUS_BYTE` is a constant) — see Decision 10.

**Nothing in the file is runnable as a gate today**, and the reason is not a shortfall in the file:
there is no 128 to run it on. Its memory map is a 48K's, so it would *load*; every number in it is
scaled by the frame-length ratio, so on a 48K it would be uniformly red for a reason already known
before running it. **The file becomes usable at implementation step 4 and not before.** It was
fetched now because the table count decides how much of the milestone can ever be *measured*, and
that answer does not need the machine.

So: **M7's contention figures are transcribed, from a *derived* source, and this milestone does not
change that.** Precisely, from the World of Spectrum 128K reference, quoted:

> *"the 6,5,4,3,2,1,0,0 pattern starts at 14361 T states after the interrupt"*, *"repeats every 228
> T states"*, *"There are 70908 T states per frame"*, *"228 T-states per scanline"*, and *"Memory
> banks 1,3,5 and 7 are contended."*

**One genuinely good property of that transcription, worth noticing because it is not automatic.**
The figure is stated **relative to the interrupt** — *"14361 T states after the interrupt"* — which
is exactly and only what `MACHINE.md` established the 48K oracle can speak to: *"the first contended
T-state falls exactly 14335 T-states after `/INT`… the origin of the frame is still a convention."*
So the 128 figure arrives in the right coordinate system, and this project's convention of asserting
`/INT` at frame T-state 0 makes it directly usable without a change of frame. A source stating it
as an absolute frame position would have needed reinterpreting, and the reinterpretation would have
been a place to be silently wrong.

**And 70908 has three transcriptions that agree** — this reference, `MACHINE.md`'s table, and the
`.z80` format description's *"17726 in 128K modes"*, whose `(17726 + 1) × 4` is 70908 exactly.
`228 × 311 = 70908` closes it, and the format description is a genuinely independent lineage from
the hardware FAQ. That figure is as solid as a transcribed number gets here.

**14361 is not, and the two should not be quoted with the same confidence merely because they
arrive in the same sentence.** The Sinclair Wiki page carrying it **cites no source at all** for
the 128 figures — checked specifically — and reads as a descendant of the World of Spectrum FAQ
rather than an independent witness. So the apparent two-source agreement is **one ancestor seen
twice**, which is the failure mode `STATUS.md` names as *"a derived figure repeated across
documents acquires authority it never earned"* — and *"copying a conclusion is the one operation
that cannot detect an error in it."* No primary posting or measurement for 14361 was found.

The 128's contention **pattern** is on firmer ground than its offset: both sources state it is the
identical `6,5,4,3,2,1,0,0`, with only the offset and the 228-T-state line period differing. That
is a claim about *sameness*, which is much harder to get wrong by transcription than a five-digit
constant.

### What the 48K oracle does and does not license us to say about the 128

One result from the 48K gate bears directly on this and is easy to over-read, so it is stated twice
in this document — once in Decision 4 where the number lives, and once here where the gate is
decided. `timing_oracle.rs`'s mutation of `FIRST_CONTENDED_T_STATE` to **14361, the 128's figure,
went red by 23 rows of 68**.

**That is strong evidence that the two machines are genuinely different, and it is not evidence of
what the 128's number is.** It is a fact about a 48K measured on a 48K oracle. The most it licenses
is the negative: *a single shared contention constant across both models is refuted*, which is
worth having — it forecloses the cheapest wrong design, and it is why `Timing` is a per-model value
in Decision 4 rather than a constant with an exception in it.

### The tiers, in `M6.md`'s vocabulary

| Tier | What it is | Evidence class | Corpus |
|---|---|---|---|
| **T1** | The paging port over all 256 values × the decode axis; the lock's absorbing behaviour; the derived slot map; the AY's structural properties (Decision 6); `Memory::bank` against the slot map; the snapshot round trips R1/R2/R3 extended to 128 fields; the hostile-input sweeps; the frame-position conversion exhaustive over **both** frame lengths | **proven** — deterministic, self-contained | none |
| **T2** | The real 128 ROMs boot to the 128's own menu; the menu's *48 BASIC* entry is selected **from the keyboard** and reaches `© 1982 Sinclair Research Ltd` **through the second ROM** | **measured** — binary pass/fail against a program we did not write | the two 128 ROMs, Decision 9 |
| **T3** | **A program we wrote that a 48K gets demonstrably wrong** | **measured** | the same ROMs |
| **T4** | A real 128-only game; one of our 128 `.z80` files opening in a third-party emulator | **observed** | user-supplied, absent by default |

**T3 is the load-bearing tier and its design is what makes M7's gate honest.** A program that writes
a signature into a bank, pages a different bank into `0xC000`, writes a different signature, pages
the first back and verifies both is genuinely 128-only: on a 48K the paging writes are absorbed and
both signatures land in the same bank, so the second overwrites the first.

**Run it on both machines and assert both outcomes.** A gate that says *"this passes on the 128"* is
much weaker than one that says *"this passes on the 128 **and fails in a specific, asserted way on
a 48K**"*, because only the second proves the paging is what made the difference. Without the
negative half, the program could be passing for a reason unrelated to paging and nothing would
show it — which is `STATUS.md`'s *"a gate can pass vacuously, and only a positive control catches
it"* applied to a milestone rather than to a probe.

**T2's keyboard step is not decoration either.** Selecting a menu entry exercises the ULA keyboard
read, the second ROM, and the ROM-select bit of `0x7FFD` in one motion, and it lands on a string
this project already knows how to assert against glyphs read from the ROM's own character set. It
is the 128's equivalent of the boot gate, and — following the boot gate's own hard-won lesson — the
**frame it lands on** is asserted, not printed.

### So what is M7's gate

**M7's gate is T1 + T2 + T3. `MACHINE.md`'s "128-only software runs" is T4.**

That is the same correction `M6.md` Decision 8 made for M6, made in advance rather than after the
fact, and the milestone table in `MACHINE.md` has been updated to match. `ARCHITECTURE.md`'s copy
of the table is owned elsewhere and still carries the old wording.

**What T4 adds that T1–T3 cannot**, stated plainly rather than apologised for:

- **A program written by someone who did not know how this emulator works.** Every tier below is
  authored here, and an expectation authored here cannot discover a misunderstanding shared by the
  code and its test. This is the same asymmetry `M6.md` measured: the third-party corpus stayed
  green under a symmetric mutation of the T-state formula, and only a hand-derivation and an
  independent implementation went red.
- **Breadth.** A commercial 128 title exercises the AY continuously, pages under interrupt, uses
  the shadow screen for double-buffering, and depends on timing in ways nobody enumerates in
  advance.
- **The AY's magnitudes.** T4 is the *only* tier in this milestone with any purchase on whether the
  sound is right, and it is a human ear.
- **Whichever `FIRST_CONTENDED_T_STATE` the 128 actually uses**, if no oracle exists for it — via
  the classic observation instrument, a multicolour effect landing one character cell out.

---

## Decision 9 — the second ROM, and a `.gitignore` glob that commits by default — **ruling**

`.gitignore` ignores all of `testdata/` and then un-ignores the ROM directory:

```
testdata/**
!testdata/roms/
!testdata/roms/*.rom
```

with the comment *"The Sinclair ROMs are the one corpus that may be redistributed — Amstrad has
explicitly permitted shipping them with emulators."*

Two questions follow and they have different answers.

### A finding that stands whatever the permission turns out to say

**This repository redistributes a copyrighted ROM on the strength of a permission whose text
appears nowhere in it.** `.gitignore`, `ARCHITECTURE.md` and `testdata/README.md` each assert that
*"Amstrad has explicitly permitted redistributing the Sinclair ROMs with emulators"* — three
copies of one claim, with **no quotation, no author, no date and no URL** between them. Compare
what the same `testdata/README.md` demands of a ROM's *bytes*: size, SHA-1, CRC-32, two independent
mirrors fetched and compared, and a stated reason (*"a ROM is the one corpus here whose contents
nothing else validates"*). The provenance of the **bytes** is documented to this project's usual
standard; the provenance of the **right to ship them** is a sentence.

That is the same shape as the defects this project catalogues — a claimed protection with nothing
behind it that a reader can check — and it is worse here in one specific way: **a wrong technical
claim produces a bug, and a wrong licensing claim produces a redistribution nobody was entitled to
make.** It is also cheap to fix, which is why it is raised rather than filed: the permission is a
short, widely-quoted statement, and recording it verbatim with its author, date and source next to
the SHA-1 costs one paragraph.

**M7 must not add a second ROM to that footing.** So the permission was looked up, and it is quoted
below with its conditions and its scope.

### The permission, quoted, with the condition this repository does not currently meet

The statement is from **Cliff Lawson of Amstrad plc**, posted to **comp.sys.sinclair on 31 August
1999**, and reproduced ever since — including in Debian/Ubuntu's [`spectrum-roms` copyright
file](https://launchpad.net/ubuntu/noble/+source/spectrum-roms/+copyright), which is a
distribution's own legal record rather than a fan page, and in
[`z00m128/zxs-rom`](https://github.com/z00m128/zxs-rom/blob/master/LICENSE.md):

> *"Amstrad are happy for emulator writers to include images of our copyrighted code as long as the
> (c)opyright messages are not altered and we appreciate it if the program/manual includes a note to
> the effect that 'Amstrad have kindly given their permission for the redistribution of their
> copyrighted material but retain that copyright'."*

Four conditions travel with it: **the copyright messages must not be altered**; **the acknowledgement
note is requested**; distribution is **for emulator use**; and the ROM code is **not to be sold**,
though a shareware fee for the emulator author's own work is explicitly fine. Modification is
permitted — *"If they choose to modify the behaviour in any way then that's entirely up to them"* —
and modified ROMs may be redistributed under the same copyright proviso.

> **Caveat on the sourcing, and it should be discharged before this goes into a licence file.**
> The canonical [World of Spectrum permits page](https://worldofspectrum.net/permits/) returned
> **HTTP 403** and could not be read, so the wording above comes from two reproductions rather than
> from the primary posting. For a *technical* claim two agreeing reproductions would be plenty; for
> a **licensing** claim the primary text is what should be quoted, because the quotation is the
> thing being relied on. Pull it from a source that can be read in full, then quote it in
> `testdata/README.md`.
>
> **Partly discharged, 2026-09-01, and one substantive correction came out of it.**
>
> - **The permits page is reachable now** — re-fetched with a browser user-agent, **HTTP 200**,
>   52454 bytes. **It does not carry the permission's text**: it says only that *"Amstrad… allow
>   free distribution of the ZX Spectrum ROMs (see the message from Amstrad as posted in
>   comp.sys.sinclair for the details)"* and points elsewhere for the wording. So the 403 was not
>   what was hiding the primary text; the page never had it. **The caveat is not discharged by that
>   page and cannot be.**
> - **A full reproduction of the posting was read end to end** —
>   [`z00m128/zxs-rom/LICENSE.md`](https://raw.githubusercontent.com/z00m128/zxs-rom/master/LICENSE.md),
>   which carries the whole thread: forum `comp.sys.sinclair`, subject *"Amstrad ROM permissions"*,
>   dated **31 August 1999**, author **Cliff Lawson `<clawson@amstrad.com>`**, as eight numbered
>   questions with his answers. The sentence quoted above is verbatim and is his answer to question
>   1. This is still a reproduction rather than an archived copy of the original article, and that
>   distinction should survive into `testdata/README.md`.
> - **Correction — the scope is *not* "enumerated" by the statement, and this document said it
>   was.** The list *"Interface 1+2, ZX80, ZX81, Spectrum 48, 128, +2, +2A and +3"* is in
>   **question 5**, not in the answer. What Lawson actually replies is hedged: *"I think Amstrad
>   only bought the rights to Spectrum 48/128 from Sinclair and then produced the + machines
>   ourselves. I do not believe the (c) for ZXs or IF1/2 has anything to do with Amstrad."* On
>   Timex (question 7): *"Ask Timex."*
>
>   **The conclusion is unchanged and the confidence in it should be lower.** Spectrum 48 and 128
>   are affirmative; the `+` machines are covered by *"produced… ourselves"* rather than by being
>   named as licensed; the ZX80, ZX81 and Interface 1/2 are **disclaimed** — with *"I do not
>   believe"*, which is a disclaimer of Amstrad's *ownership*, and is therefore a stronger reason
>   not to ship those ROMs than a licence hedge would be: if Amstrad does not hold the rights, no
>   Amstrad permission can reach them **however** it is worded. **The `.gitignore` argument
>   therefore stands on the disclaimer rather than on the enumeration**, which is a firmer footing
>   than the one this document originally gave it.

**Scope covers the 128's ROMs, and it is enumerated rather than inferred.** The statement names the
covered machines: **Spectrum 48/128, +, +2, +2A and +3**. It also names what it does **not** cover —
**the ZX80, the ZX81, and the Interface 1 and Interface 2 ROMs**, which Amstrad does not hold the
rights to, along with third-party clones such as Timex. Corroboration that this is how it is read in
practice: Debian/Ubuntu's `spectrum-roms` ships `48.rom`, **`128-0.rom` and `128-1.rom`**, the
Spanish 128 variants and the `plus2`/`plus3` images under exactly this copyright file, and a
distribution's licensing review accepted it.

**So committing the two 128 ROMs stands on precisely the same footing as the 48K ROM already
does** — and the exclusion list is not a footnote, because of what it does to the next paragraph.

> **The exclusions make the glob argument concrete rather than hypothetical.** An `if1-2.rom`
> dropped into `testdata/roms/` matches `!testdata/roms/*.rom` and would be committed — and
> **Interface 1 and Interface 2 ROMs are named in this permission as ROMs Amstrad cannot grant
> rights to.** That is no longer a story about a file that might someday have a different licence;
> it is a specific, plausible file, of a kind an emulator project acquires in the normal course of
> its work, which the pattern would commit and the permission does not reach.

> **Two corrections to this document, made where a reader will meet them.**
>
> **The +3 claim below was mine and I did not check it.** The scope paragraph in Decision 10's
> neighbourhood asserted that *"the +3's ROMs are commonly reported to have a different status from
> the 48K's and the 128's."* **The evidence found points the other way** — `spectrum-roms` ships
> `plus3` images under the same permission. The claim is withdrawn. It is left visible rather than
> deleted because it is a clean example of the failure this document opens by warning about: a
> confident, plausible, licence-shaped sentence written from familiarity, in the very section
> arguing that licensing claims must be quoted rather than asserted.
>
> **The argument for replacing the glob does not depend on it and is unchanged.** The problem with
> `!testdata/roms/*.rom` was never that some *Sinclair* ROM has a different licence. It is that the
> pattern accepts **any** file ending in `.rom` — a game cartridge, a third-party interface ROM, a
> Multiface image — none of which this permission touches at all. The glob turns "may we
> redistribute this?" from a decision into an accident, and that is true whatever the +3's status
> turns out to be.

**And the acknowledgement note is missing.** The permission asks that *"the program/manual includes
a note"* in specific terms, and this repository has no such note anywhere: `ARCHITECTURE.md`,
`.gitignore` and `testdata/README.md` each state that *permission exists*, which is a different
thing from making the acknowledgement the permission requests. It is a *"we appreciate it if"*
rather than a hard condition, and it costs one sentence in `README.md`. **M7 should add it in the
same change that commits the second and third ROM**, because a repository that is about to double
the number of ROMs it ships is exactly the wrong moment to still be skipping the courtesy the
permission asked for.

### Should the 128 ROMs be committed?

**Yes, if the permission's scope covers them**, and for the reason `M6.md` Decision 8 gives: *"a
gate whose corpus is absent by default is a gate that runs nowhere."* T2 and T3 both need the ROMs,
and a committed
corpus is what makes them run on every clone. This is also the consistency answer: the 48K ROM is
committed under the same permission and there is no principled line between them.

**Should the glob stay?** **No, and this is a real defect that M7 exposes rather than creates.**
`!testdata/roms/*.rom` is *permissive by default*: any file ending in `.rom` dropped in that
directory is committed with no further thought. ~~The licensing status of Spectrum ROMs is **not
uniform** — the +3's ROMs are commonly reported to have a different status from the 48K's and the
128's~~ — **withdrawn, see the correction above; the evidence points the other way** — but a game
cartridge, a third-party interface ROM or a Multiface image is not covered by Amstrad's permission
at all, and each of them ends in `.rom`. A glob turns "may we redistribute this?" from a decision
into an accident.

**Replace the glob with explicit filenames.** Then adding a ROM requires editing `.gitignore`,
which is exactly where the licence check belongs and exactly where a reviewer will see it. The cost
is one line per ROM, forever; the thing it prevents is a redistribution nobody decided to make.

`.gitignore` is not this document's to edit; it is implementation step 1 below, and
`testdata/README.md` gains a 128 section in the shape its 48K one already has — size, SHA-1,
CRC-32, two independent mirrors compared byte for byte, and the absence-policy table.

---

## Decision 10 — scope — **ruling (YAGNI)**

| | Verdict | Reason |
|---|---|---|
| **+2 (grey)** | **not a model.** Free if someone supplies the ROMs | Electrically a 128 with a different case and a built-in tape deck: same `0x7FFD`, same AY, same frame. It is a *ROM pair*, not a machine variant, and treating it as one would add a `Model` variant that differs from `Spectrum128` in nothing the emulator models |
| **+2A / +3** | **out**, confirming `ARCHITECTURE.md` rather than re-litigating it, and the survey **strengthens** the case | *"They add port `0x1FFD`, all-RAM configurations and yet another contention pattern, for a sliver of extra software."* All three are new mechanisms, not new data — and the contention row is worse than "another pattern" suggests: an Amstrad gate array rather than a Ferranti ULA, contending pages **4, 5, 6, 7** (not 1, 3, 5, 7), with the pattern **`7,6,5,4,3,2,1,0`** (not `6,5,4,3,2,1,0,0`), applied only while `/MREQ` is active. Every column of Decision 1's difference table would need a third value, and the last of those is a *mechanism* change to `Ula::tick`, which is the one function this milestone leaves alone |
| **The +3 disk interface** | **out** | Depends on the +3. A floppy controller is a milestone, not a decision |
| **`.szx`** | **out** — Decision 7 | M6 deferred it *to* M7; the reason it gave does not survive the format |
| **`.tzx`** | **out**, and it is the first thing to add if T4 fails | Unchanged from `M6.md`, and the pulse-train decision makes it a converter rather than a rewrite. Most commercial 128 titles are turbo-loaded |
| **The beeper** | ~~**out** — M8, per `M6.md`~~ **IN, and it is M7's.** Ruled 2026-09-01; `M6.md`'s row is struck there too | It is bit 4 of a `0xFE` write, decoded by the same `Ula::out_port` that already takes the border out of the same byte. A ULA feature belongs to the crate that models the ULA. Decision 6 carries the reasoning and the one constraint it imposes: the AY's frame hash covers the AY alone, so the two sources stay separable |
| **Floating bus** | **out**, unchanged and for the unchanged reason | No oracle for the byte-to-phase mapping. It is worth noting the 128's is *different* from the 48K's, so a 48K model would not have transferred anyway |
| **Progressive drawing** | ~~**out**~~ **the border half is IN; the bitmap and attribute halves stay out.** Ruled 2026-09-01, on the owner's report that a tape load's stripes were wrong | The reason the row said *out* was that progressive drawing *"needs the frame's write history keyed by T-state, which is a different data structure and a different verification story."* That is true of the **bitmap** — 6912 bytes, keyed by T-state — and it is not true of the **border**, which needs one slot per rendered row and cannot overrun because a guest cannot create rows. The two are not the same problem and doing the cheap half is not starting the expensive one. Resolution is one scanline, **derived**: vertically the frame buffer maps to hardware exactly, horizontally it does not (`BORDER` is a uniform 32 px where the hardware's is not), so a T-state-to-column mapping would invent precision the buffer cannot carry. Measured against the effect: the real ROM's loader changes the border every **1884–2159 T-states** — a band every **8.4 to 9.6 rows** — and the rendered result is **27 bands of 9–10 rows**, red and cyan |

---

## Decision 11 — the three resisting oracle rows, and why no constant closes them — **ruling: change nothing**

Added 2026-09-01, after Decision 8. `timing_oracle.rs`'s module documentation records that with
`INTERRUPT_T_STATES` at 33 the machine becomes `TYPE2` and **three of the 68 rows resist**, and it
named two things that would settle them: a `TYPE2` machine's full 73-row hardware submission, or an
interrupt-sampling model finer than one T-state. **Both routes were taken. Both are closed, and the
ruling is that nothing should move.**

### The suspicion that prompted this, stated so it can be seen to be answered

The residue looked like a defect in **when this machine samples `/INT`**. `Spectrum::step` tests the
line at the instruction *boundary*; a Z80 is documented to sample it during the instruction's **last
T-state**. If that one-T-state difference were real and uncorrected it would touch every interrupt
gate, the 128, and any interrupt-synchronised software — so it was worth settling properly rather
than absorbing.

### Route 1 — the hardware results database holds no rows, and never did

`spectrum48k_timing_results.htm` returns **HTTP 200** and is a single **39-row, 8-column** table:
Model, Z80, Board Issue, ULA, Serial, **TYPE**, Notes, Submitter. It publishes each machine's
*verdict* and provenance and **not one of the 73 measured values**. The 2010 Internet Archive copy
has the identical column structure, so the rows were never published rather than published and
withdrawn; and `submit_test_results.php` offers a **600-character free-text box**, which could not
have carried them. The counts this project already cites do reconcile against it — 28 submissions,
9 people, 17 `TYPE1`, 8 `TYPE2`, 2 clones, 1 undetected.

**This is a structural absence, not a fetch failure, and it will not change by looking again.** The
consequence is worth stating plainly: **the suite's own embedded `TYPE2` table is the strongest
artefact that exists**, and `timing_oracle.rs` already reads it. There is no finer-grained external
check to be had for these rows.

### Route 2 — a finer sampling model has no extra expressive power, and the sample point is a gauge

Whatever rule a Z80 uses inside a T-state, its **only** observable consequence here is the set of
instruction-boundary positions at which `/INT` is accepted; for an interval `/INT` that set is an
interval `[lo, hi]`. So imposing `[lo, hi]` **directly**, with the contention constant untouched,
sweeps the whole family that any sub-T-state model could produce. Over `lo` in `0..=4` and `hi` in
`31..=40`:

| `lo` | class boundary | vs `TYPE1` | vs `TYPE2` |
|---|---|---|---|
| **0** | `TYPE2` from width 33 | **0 of 68** | **3 of 68** |
| 1 | width 33 | 8 | 6 |
| 2 | width 33 | 13 | 8 |
| 3 | width 33 | 15 | 12 |
| 4 | width 33 | 13 | 14 |

Two things fall out, and both are measurements. **`hi` matters only through the width** — every
width ≥ 33 is `TYPE2` and no width above 33 changes a single row, because the amplifier needs
exactly one boundary at `+32`. **`lo = 0` is the unique minimum against both tables**, and it is
8-way sensitive: one T-state either way reddens rows that are green now. **No window of any position
or width reaches zero against `TYPE2`. The floor is three, and it is these three.**

**And the sample point is a gauge paired with `FIRST_CONTENDED_T_STATE`, which was measured rather
than argued.** Sampling at the boundary with contention at **14335**, and sampling at the
instruction's last T-state with contention at **14336**, agree on all 68 rows *and* the detection
row, at width 32 (`TYPE1`, 0 of 68) and at width 33 (`TYPE2`, the same 3 rows with the same values).
Only the *difference* between the two windows is observable. So **"this machine samples `/INT` at the
wrong point" is not a well-posed defect against this corpus**; and if the Z80's real rule is the last
T-state, then the constant that should read 14336 is the *contention* one and nothing else moves.

### What the three rows are: right instruction timing, a different accepting boundary

Each hardware `TYPE2` triple is an instruction boundary that **exists in this machine's own trace**
with exactly the right `R`, calibrated on rows matched bit-exactly (`R` recorded is `R` at
acceptance `+ 4`; the third field is the interrupted `PC` wherever it varies):

| row | hardware wants | the boundary in our trace | where we accept |
|---|---|---|---|
| 3 contended | `R` 41, `0x5B0B` | `r` 37 at `pc 0x5B0B`, frame T-state **69887** | 69888 + 3 |
| 7 uncontended | `R` 98, `0xDDDD` | `r` 94 at `pc 0xDDDD`, frame T-state **+20** | +0 |
| 34 uncontended | `R` 44 | `r` 40 at `pc 0x0082`, frame T-state **+16** | +4 |

So the per-instruction and per-contention accounting inside these three groups is **correct to the
T-state** — which the 68 bit-exact `TYPE1` rows say independently. The disagreement is entirely
about *which* boundary takes the interrupt.

**Their demands are mutually inconsistent.** Writing `φ` for a uniform shift of a group's boundaries
against the window: group 3 needs `φ` in `+1..=+4`, group 7 needs `−20..=−1`, group 34 needs
`−16..=−10`. Seven and 34 intersect; **3 is disjoint from both and wants the opposite sign.**
Meanwhile the detection row pins the `TYPE1`→`TYPE2` entry shift to **exactly 32** — `R` = 122
requires entry 324 and no other value — which is `φ = 0`. **Four constraints, no common solution.**

### The third possibility, tested rather than assumed

That the three cells are simply wrong is the remaining explanation, and it is **weaker than it
looks**. The `TYPE2` column's three most suspicious entries are reproduced by this machine
*bit-exactly*: group 29 contended `sp` = **0** (which reads like missing data and is not), group 11
contended `sp` = **53256** (a far outlier against every other contended `sp` of ~23300), and group
15's loop count roughly **doubling** between the classes (553→1100, 462→919). **A table whose oddest
entries an independent implementation lands on is not a carelessly transcribed table.** The three
rows are therefore *unsettled*, leaning toward a real sub-T-state property of a `TYPE2` board that an
integer-T-state acceptance window cannot express — not toward transcription error, and not toward a
defect in this emulator's instruction timing.

### The ruling, and the tension it dissolves

**`INTERRUPT_T_STATES` stays at 32 and `FIRST_CONTENDED_T_STATE` stays at 14335.** The apparent
tension — a sweep pinning the constant to `17..=32` while "the oracle wants 33" — is not a tension at
all, and saying so is the useful half of this decision: **at 32 the oracle is entirely green.** The
detection row is bit-exact `TYPE1` and all 68 rows match the `TYPE1` table. 33 does not fix anything
that is broken; it *changes which real machine this is*, from the majority class (17 of 25 submitted
boards) to the minority (8 of 25), and buys three mismatches for the privilege. **The evidence
favours 32 on both counts, and the three rows are a property of `TYPE2` hardware this model cannot
reach — not a reason to move a constant.**

**What would still settle them** — the honest residue, now that the two obvious routes are closed —
is a `TYPE2` machine on a bench with a logic analyser on `/INT` and `M1`, or a gate-level ULA model
paired with a sub-T-state Z80 core. Neither is a software question and neither is in scope for M7.

---

## Public API delta

Every item is a semver event and needs approval.

> ## **Approved — 2026-09-01. One breaking change and eleven additions.**
>
> This section is no longer a request. It is recorded here rather than left to be inferred from
> an absence of objection, because *"reporting the absence of a distinguishing test as evidence of
> correctness"* is this project's standing lesson and the absence of a refusal is the same shape.
> Four of the twelve were approved with reasoning that is worth keeping, because in each case the
> reasoning is what makes the item **not** a judgement call:
>
> - **`Spectrum::restore` → `Result<(), ModelMismatch>`.** Approved. Silently dropping five banks
>   is exactly the defect M6 refused for duplicate pages, and **the rule has to be symmetric**: a
>   48K image loaded into a 128 runs against the wrong ROM, which is the quieter half and the one
>   easier to talk yourself out of. Re-taken here rather than quoted: `grep -rn '\.restore(' crates/`
>   on 2026-09-01 returns **14** call sites in **two** files, `crates/spectrum/tests/snapshot_apply.rs`
>   and `crates/spectrum/tests/tape_signal.rs`. All are in-workspace, `crates/frontend/src/main.rs`
>   is `fn main() {}`, and nothing is released. *(The table below records 13, taken earlier the same
>   day. Neither figure is wrong; both are counts of a moving tree, which is why each carries its
>   command and its date instead of standing alone.)*
> - **`Memory::bank` / `bank_mut`.** Approved. Three callers, not the one `M6.md` predicted — the
>   applier, the snapshot **writer**, and `screen::render`.
> - **`z80::Bus::acknowledge(addr, t_states)`, defaulted.** Approved, **and the reasoning is the
>   part to carry forward.** The earlier ruling not to fix the per-T-state acknowledge rested on
>   *"no test can tell the two models apart"*. That was true of **software running on the machine**
>   and false of **a test driving the bus**, which has existed since M5. So the fix closes a model
>   defect and closes **no** verification gap — both halves go in the register, and the second half
>   is not a caveat on the first. It follows the `fetch` precedent and is non-breaking by
>   construction.
> - **`timing::Timing`.** Approved **as an addition, with the existing constants redefined from
>   it**, so SSOT is compiler-enforced and **zero public items are removed**. That matters more
>   than it looks: those constants are the positioning device for **14 of the 19** test files
>   (`grep -rln 'FIRST_CONTENDED_T_STATE\|T_STATES_PER_FRAME\|INTERRUPT_T_STATES'
>   crates/spectrum/tests/ | wc -l` against `ls -1 crates/spectrum/tests/*.rs | wc -l`, recorded
>   with its date in Decision 4 — **run it, do not quote it**), and replacing them would rewrite
>   fourteen correct 48K gates to say a 48K fact in a more general vocabulary.
>
> The remaining eight additions and the `pub(crate)` list are approved as written and are not
> re-argued here.

### Breaking — one item

| Change | Why it is unavoidable |
|---|---|
| `Spectrum::restore(&Snapshot)` → `Result<(), ModelMismatch>` | A 128 snapshot restored into a 48K has five banks with no address. Dropping them silently is the defect `M6.md` refused for duplicate pages. All callers are in `crates/spectrum/tests/` (`grep -rn '\.restore(' crates/` returned **13**, 2026-09-01, and the count is rising as M6's tests land); there are none outside the workspace, and `crates/frontend/src/main.rs` is `fn main() {}` |

### New public items

| Item | Why it is unavoidable |
|---|---|
| `Model` (`#[non_exhaustive]`, `Spectrum48K` / `Spectrum128`) | `Snapshot` must say which machine it describes; `M6.md` predicted this exact variant |
| `Snapshot::model` | as above; the parser is where the model is decided |
| `Spectrum::spectrum_128(&editor, &basic)` | the milestone's subject |
| `Memory::spectrum_128(&editor, &basic)` | as above |
| `Memory::bank(BankIndex) -> &[u8; PAGE_SIZE]` / `bank_mut` | **confirmed unavoidable, and with more callers than `M6.md` predicted.** `M6.md` named one — the snapshot *applier* writing a bank that is paged out. There are three: the applier, the snapshot **writer** (`Spectrum::snapshot` currently walks `exposed_banks()` and reads through addresses, which on a 128 captures three banks of eight), and **`screen::render`** reaching the shadow bank (Decision 3). `exposed_banks` stops being the writer's iterator and becomes what it is named for |
| `Memory::screen_bank() -> BankIndex` | `screen::render` must ask which bank the ULA is drawing; the alternative is a second copy of the bit-3 rule |
| `timing::Timing` + `SPECTRUM_48K` / `SPECTRUM_128` + `Timing::delay` | Decision 4. The existing constants are redefined **from** it, so this is an addition and not a replacement |
| `snapshot::ModelMismatch` (`#[non_exhaustive]`) | the applier's typed failure |
| `ay::Ay` and its register accessors | the snapshot must read and write the register file and the select latch. **Fifteen registers, sixteen bytes in the format** — Decision 6; the accessor's signature is where that mismatch is either handled or hidden |
| `Spectrum::ay()` / `ay_mut()` | as above |
| `z80::Bus::acknowledge` (**defaulted**) | Decision 5. Non-breaking by construction, the `fetch` precedent |

### New `pub(crate)` items — public surface unchanged

`Memory::write_paging_port`, `Memory::set_paging_port` (the lock-bypassing restore setter),
`Memory::paging_port`, `Memory::reset`, `Ula::ay_mut`, `Snapshot::set_model`.

### Deliberately absent, and non-breaking to add later

`Memory::paging_locked()` and `Memory::rom_index()` (derivable from the port byte, and no caller);
`Spectrum::model()` (nothing asks); `Ay::sample_rate` / any resampling (**M8 owns the audio
*device*, and the resampling to a host rate that goes with it — it no longer owns the beeper or
the AY, which are the machine's; ruled 2026-09-01, see Decision 6**);
`snapshot::szx`; `sna::write`; everything on the `M6.md` list that M7 does not need a caller for.

`CHANGELOG.md`'s scope sentence already widened at M6 to cover `spectrum`; M7 is the first
milestone since `fetch` to move `crates/z80`'s surface, and `acknowledge` belongs in the same entry
as the `Bus` trait's other additions.

---

## What nothing covers

`MACHINE.md` asks for this list explicitly, and `STATUS.md`'s standing rule is that *"reporting the
absence of a distinguishing test as evidence of correctness is the failure this project keeps
catching."*

| Property | Covered by |
|---|---|
| **The 128's `FIRST_CONTENDED_T_STATE`** | **Nothing that reaches *measured*.** The 128 suite (Decision 8) can pin it and can refute it; it cannot establish it, because there is no 128 hardware results database. 14361 is a **single-ancestor derivation** citing no primary source |
| The 128's contention **pattern** — the `6,5,4,3,2,1,0,0` and the 228-T-state period | **derived**, from the same two sources with the same single ancestry. The 48K's pattern is now hardware-graded; none of that transfers |
| The AY's **port decode rule** — which address lines select `0xFFFD` and `0xBFFD` | **nothing, and no source for it was found.** Inferred from the two addresses; the weakest claim in this design |
| `R15`, which `.z80` v3 carries and the `-8912` does not have | **nothing** — a round trip is blind to it by construction |
| The frame's **origin** on a 128 | **nothing**, and it is the same convention the 48K has — `/INT` at T-state 0. A machine asserting one T-state later with the window moved to match is indistinguishable |
| The interrupt window's **length** on either machine | **nothing.** Ungraded on the 48K — a 32 → 24 mutation left the timing oracle green — and **inherited unmeasured by the 128, which may not even use the same figure.** It is a `Timing` field precisely so that a 128 value different from 32 is a data change and not a code change; nothing here establishes either number |
| The **interrupt acknowledge**'s contention | **nothing, and nothing can** — Decision 5 argues no software on either machine reaches the case. The model is pinned, not graded |
| The **`0x7FFD` address decode** beyond `0x7FFD` itself | **nothing** unless software reaches the port another way. Transcribed from a source, graded against the transcription |
| The **AY's magnitudes** — volume table, envelope divisor, tone prescaler | **nothing.** Decision 6 |
| Whether the AY **sounds right** | **T4**, a human ear |
| AY write **timing within the frame** | **nothing.** The audible result depends on it; no gate does |
| The **floating bus** on a 128 | **nothing** — not modelled, and its mapping differs from the 48K's |
| **Progressive drawing**, so the shadow screen mid-frame | **nothing** — unchanged from M5 |
| Our **reading** of the 128 `.z80` extensions | **only** a third-party emulator opening one of our 128 files — T4, manual, and M6 measured that a foreign file proves a field *readable* and says nothing about our arithmetic on it |
| A real 128 **game** | **T4**: observation, corpus-dependent, absent by default |

---

## Implementation order

Each step is independently reviewable and each has a gate that goes red without the next.

0. ~~**Fetch `timing_tests-128k_v1.0.z80` and count its expected-result tables** (Decision 8). Step
   zero because it is minutes of work and it decides how much of this milestone can ever be
   *measured*: one table confirms the structural argument and caps the artefact at *derived*; two
   would be a genuine surprise worth investigating properly. Either way the file is worth running,
   and either way the answer belongs in the register before anything is built on it.~~
   **Done, 2026-09-01 — one table, at `0xE200`.** The file is in `testdata/timing/`, gitignored
   and routed through `crates/testsupport` like every other corpus; its size, both hashes and its
   provenance are in *Step 0, taken* under Decision 8, along with the `0xE400` trap that step
   4 must not walk into. The step is left struck rather than deleted because the *reason* it was
   step zero — that the answer caps the milestone's evidence tier — is the part worth carrying,
   and it is now an answer rather than a plan.
1. **`.gitignore` explicit filenames, the two 128 ROMs, `testdata/README.md`, and the Amstrad
   acknowledgement note** (Decision 9). Nothing else can be gated until the corpus exists, and the
   acknowledgement is a one-sentence debt the repository already owes on the ROM it ships today.
2. **`Memory`: the paging port, the lock, `bank`/`bank_mut`, `screen_bank`, `reset`, and
   `spectrum_128`.** Gate: all 256 port values; the lock's absorbing behaviour; the 48K unchanged
   under `paging_port_at_reset = 0x20`; `bank` agreeing with `read` for every bank the slot map
   exposes.
3. **`timing::Timing`, with the existing constants redefined from it** (Decision 4). Gate: the
   existing `timing` unit tests unchanged and green — that is the whole assertion — plus the frame
   lengths and their `.z80` quarters.
4. **Per-bank contention and the screen bank** (Decision 3). Gate: `contention_magnitude.rs`'s
   existing cases re-run with a contended bank at `0xC000`; bit 3 changing what `render` draws and
   nothing else; the contended set not following the screen select. **And run the 128 timing suite
   fetched in step 0**, recording its verdict either way — a green is worth little (Decision 8) and
   a red is worth a great deal, so it must be run before anyone has an opinion about the outcome.
5. **`Spectrum::spectrum_128`, `Model`, and T2** — the 128 ROMs to their menu, the keyboard
   selection, the 48K message through the second ROM, **and the frame each lands on**.
6. **T3** — the bank-signature program, asserted to pass on a 128 **and to fail in the asserted way
   on a 48K**.
7. **`Bus::acknowledge` and `Ula`'s implementation** (Decision 5). Gate: a direct `Cpu<Ula>` at a
   contended frame position with `IR` in a contended bank, separating the two models; plus the
   assertion that no 48K figure anywhere moved.
8. **The AY**: register file and masks, port decode, the three generators, the mixer, the sample
   stream (Decision 6). Gate: the structural table, exhaustively; the frame hash over the AY's own
   output.
9. **`.z80` 128 support**: the model-dependent hardware-mode table, the paging byte, the AY
   registers, eight banks, `Snapshot::model`, fallible `restore`, and the redesigned bank-set guard
   (Decision 7). Gate: R1/R2/R3 over 128 snapshots; a hand-transcribed 128 vector per version; the
   frame-position conversion exhaustive over 70908 **and** 69888, in one gate that loops over both
   models rather than two that each see one machine.
10. **Documentation, as a step and not a tidy-up.** `STATUS.md`'s registers (see below),
    `CHANGELOG.md`, `README.md`'s milestone row, `testdata/README.md`, and the doc-comment sweep at
    the milestone boundary — which for M7 specifically means `ula.rs`'s
    *"Interrupt-acknowledge contention — charged seven times, not once"* row and the paragraph under
    it, `memory.rs`'s *"marking those here would be a claim about hardware a 48K does not have"*,
    `timing.rs`'s *"this crate has no oracle for it"* and its *"A 128 runs 70908, which is why this
    is a constant to be moved"*, and `lib.rs`'s coverage table.

> **Step 10 is the step M6 skipped**, and it is worth naming rather than assuming. `M6.md`'s
> implementation order carries the same step, its own text calls it *"a step and not a tidy-up"*,
> and `STATUS.md` has not been written to since M5 — its **first open row still says
> `FIRST_CONTENDED_T_STATE` has no oracle**, which `MACHINE.md` closed. M7 should not be started on
> top of a register that is a milestone behind, and reconciling it is not M7 work: it is M6's step
> 10, unfinished.

---

## Open items this milestone hands to the register

`STATUS.md` owns the open register and this document does not duplicate it.

| Item | Settled by |
|---|---|
| **14361 rests on a single ancestor.** The Sinclair Wiki cites nothing for it and reads as a descendant of the World of Spectrum FAQ, so the apparent two-source agreement is one source seen twice | a primary posting or measurement, which was searched for and not found; or the 128 suite disagreeing with the machine, which would be a real finding |
| The 128's contention pattern is assumed identical to the 48K's `6,5,4,3,2,1,0,0` | the 128 suite, which grades the pattern and the offset jointly |
| ~~**Whether `timing_tests-128k_v1.0.z80` carries one expected-result table or two** — this is what caps its evidence tier~~ | **Settled 2026-09-01: one.** The classification lines are deleted from its BASIC, `PEEK 40004` can never be 1, and the `0xE400` table is the 48K `Late` table byte for byte. Step 0, above |
| **The `0xE400` table in the 128 file is a 48K leftover and reading it compares a 128 against 48K hardware** | nothing to settle — it is a hazard, not a question. It is closed by the 128 reader using `0xE200` only, and it is recorded in `timing_oracle.rs` where the next person will meet it |
| **What separates the 48K suite's two classes on an *uncontended* group** — its detection rows differ by 120 in `R`, which a one-T-state contention shift cannot explain | deriving it from the detection group's own code, which is in the file. **Not attempted here**, and it is worth attempting before the 128 suite is first run, because it decides how that run's detection row is read |
| No 128 hardware results database exists, so the 128 suite cannot reach *measured* | nothing available. Recorded as a permanent asterisk, not a to-do |
| Patrik Rak's `ptime` / Weiv's `Ptime-128` (128 screen-switching timing) were never evaluated | fetching them from the SoftSpectrum 48 corpus |
| The interrupt acknowledge is pinned and **cannot** be graded — no software on either machine reaches it | **nothing, permanently.** This row closes as a question and stays open as coverage |
| The AY's magnitudes are transcribed and ungradeable in-repo | measured register/output traces from real hardware, if any exist |
| The AY frame hash proves change, not correctness | nothing available; it is the right instrument for the output it grades |
| `0x7FFD`'s decode is sourced (*"bits 1 and 15 reset"*) but from **one** source; the Sinclair Wiki and Scratchpad copies are almost certainly derivative of the same FAQ | a genuinely independent second source, or software observed to depend on it |
| The **AY's** decode has no source at all and is inferred | the same |
| A 128 `.z80` of ours has never been opened elsewhere | T4, manual, **recorded** — file, SHA-256, emulator, date |

---

## What this document could not settle

- **Whether the 128's contention figures are right.** The design isolates them in one value
  (Decision 4) so there is one place to correct, which is what `timing.rs` did for the 48K and what
  made the eventual oracle a one-line adjudication. That is the most a design can do.
- ~~**Whether the 128 suite carries one expected-result table or two.** It was located but not
  downloaded, and one indirect note (*"must be run with late timings in 48K mode"*) hints at one.
  This is the cheapest high-value question left and it is implementation step 0.~~
  **Settled: one.** The file was fetched and read on 2026-09-01 — see *Step 0, taken* in
  Decision 8. The indirect note was right and for the right reason, and the second table that
  *appears* to be there is the 48K's `Late` table copied forward unchanged. The prediction that
  leg (3) would be structurally unavailable is confirmed, so the artefact is capped at *derived*
  permanently rather than pending.
- **The AY's address decode.** `0x7FFD`'s decode is settled — quoted, *"will respond to any port
  address with bits 1 and 15 reset"* — but **no source was found stating which lines decode
  `0xFFFD` and `0xBFFD`.** Decision 2's overlap table infers them from the two addresses, the
  overlap it predicts with the ULA follows from that inference, and the *shape* of the fix (two
  independent `if`s, AY read before the floating-bus fallback) is right whether or not the mask is.
  Named as this design's least-supported claim.
- ~~**The full primary text of the Amstrad permission.** Two reproductions agree and enumerate the
  scope; the canonical World of Spectrum permits page returned 403. For a licensing claim the
  quotation *is* the thing relied upon, so it should be pulled from a readable primary source
  before it goes into `testdata/README.md`.~~
  **Partly settled 2026-09-01, and it corrected this document.** The permits page is reachable
  (200) and **does not contain the text at all** — it points at `comp.sys.sinclair`. A full
  reproduction of the thread was read end to end; the quotation is verbatim, dated 31 August 1999,
  by Cliff Lawson of Amstrad. **But "enumerate the scope" was wrong**: the machine list is in the
  *question*, and the *answer* is hedged (*"I think"*, *"I do not believe"*). The ZX80, ZX81 and
  Interface 1/2 are disclaimed as **not Amstrad's copyright at all**, which is why the
  `.gitignore` change stands on firmer ground than the sentence that motivated it. See the
  discharge note under Decision 9. **Still outstanding:** an archived copy of the original
  article rather than a reproduction of it.
- **Whether the AY's structural gates are worth their weight** if no 128-only software in reach
  uses the chip heavily. They are cheap, and the argument for them is that a wrong AY is silent —
  in both senses.
- **Whether T4 is reachable at all.** Most commercial 128 titles are turbo-loaded and `.tap` cannot
  carry them, which is `M6.md`'s unresolved question inherited unchanged. If the answer is no,
  `.tzx` moves from *later* to *required*, and Decision 5 of `M6.md` is what makes that cheap.
- **Whether `Spectrum::restore` should reconfigure the machine to the snapshot's model** rather
  than refuse. Refusing is chosen because reconfiguring needs the other model's ROMs, which the
  machine does not hold. A frontend that owns both ROM pairs could reasonably do it at its own
  level, and if one ever does, this ruling should be revisited rather than worked around.

---

## Step 8 taken, and what the sound half found — added 2026-09-01

Decision 6 asked for the AY's structure gated exhaustively, every magnitude stated as
untested, and the two not blurred. This section records what that turned into, **including the
three places the design's own numbers were wrong** and the one place it was right for a reason
it did not know.

### The dividing line held, and it is where the failures show

| | Verdict |
|---|---|
| The noise register's period | **proven**, and the blind spot **reproduced rather than quoted** — see below |
| The envelope's aliasing | **proven**, and *derived* rather than tabulated: `Envelope` implements `CONT`/`ATT`/`ALT`/`HOLD`, so the collapse of sixteen values into eight emerges and the test grades the decode. Which two upper shapes they alias onto is asserted separately, because that half is a transcription |
| Counter periodicity over all 4096 tone values | **proven**, and again in the sample stream |
| Mixer polarity | **proven**, as a boolean |
| Register write masks | **derived** — graded against the transcription, and worth it anyway because software reads registers back |
| Every magnitude | **ungraded, and each carries its source** |

### The LFSR sweep, re-taken rather than cited

Decision 6 reports that sweeping the sixteen tap pairs `(0, t)` for a 17-bit LFSR leaves
**t = 3, 5, 6, 11, 12, 14** maximal, so a period test kills ten and cannot separate the
remaining six. `ay.rs`'s own test re-runs that sweep rather than quoting it — *"copying a
conclusion is the one operation that cannot detect an error in it"* — and it reproduces the
same six. **Mutating the tap from 3 to 5 confirms the blind spot is real and not a story: the
period test stays green and only the frame hash goes red.**

### Three magnitudes were wrong, and none of them was catchable here

**This is the part Decision 6 predicted and it is worth reading as a confirmation of the
design rather than as a list of mistakes.** Every structural gate above is *relative* — a ramp
of sixteen monotonic levels, a wave whose period scales with its register, a mixer that is
active low — and every one of them passes at any divisor and either polarity. What caught
these was reading sources, which is the only instrument this milestone ever had for them.

1. **The envelope divisor was sixteen times too slow.** The datasheet's
   `f_envelope = f_master / (256 x EP)` is the frequency of a whole sixteen-step **cycle**, not
   of one step. Reading it as the step rate makes every envelope sixteen times too slow — an
   attack that should take a twentieth of a second takes most of a second, which is audible as
   an instrument that never arrives. **The YM2149 datasheet page 7 settles it** (counter clock
   is 1/32 of the repetition period, counted 32 times per pattern); **GI's own document
   contradicts itself** — §3.5.1 and §3.5.2 read in sequence yield 4096 × EP — so the constant
   carries the conflict beside it, or someone will read §3.5.2 and "fix" it back.
2. **The noise generator's output bit is inverted on the silicon**, and MAME and `ayumi` both
   emit it uninverted. From the transistor-level analysis of the die
   (`lvd2/ay-3-8910_reverse_engineered`, `rtl/ay_model.v`): `assign noise = ~noise_reg[16]`.
   It is **not cosmetic** — the mixer is `(tone | tone_off) & (noise | noise_off)`, so it
   changes the mixed output bit for bit. `jt49` agrees with the silicon. This implementation
   follows the die and departs from the two most-copied emulators, deliberately.
3. **MAME's *"this was verified on AY-3-8910 and YM2149 chips"* is not evidence.** It arrived
   in MAME 0.72 (2003) with no changelog, no named author and no method, one release after the
   algorithm itself changed from `bit0 ^ bit2` to `bit0 ^ bit3` as an unlogged one-hex-digit
   edit. Its conclusion is right and the 2019 die work is what makes it right. **It has been
   copied verbatim into 414 public repositories**, so a wiki or an emulator carrying it is a
   restatement of one unattributed comment rather than a second witness — `STATUS.md`'s *"a
   derived figure repeated across documents acquires authority it never earned"*, at a scale
   worth naming.

**And one guard deliberately not copied.** The die's feedback term carries
`| (~|noise_reg)`, forcing a `1` in when the register is all zeros — a **power-on escape**,
since reset clears the register and without it the channel would never start. The feedback map
is linear and invertible, so zero's only preimage is zero: this model seeds non-zero and a test
walks the whole 131071-step period asserting it never arrives. Copying the clause would be code
with no reachable failing case. It is named in the source and the test is what discharges it.

### `R15`: the ruling, and why a round trip cannot hold it

The `-8912` has fifteen registers and `.z80` version 3 reserves sixteen bytes. **`Ay::register`
returns `Option<u8>`**, so the mismatch is handled at the call site rather than hidden inside
one; a guest's read of `R15` gets the floating bus, because that is what nothing driving the
bus produces. **The writer emits zero at offset 54** — the byte describes a register the
machine does not have, and zero is what this writer already emits for every other field
describing absent hardware. A round trip over a file *we* wrote compares our zero against our
zero, so it is gated by assertions on the **bytes** and on the chip, never by the round trip.

### The frame hash is over the AY alone, and the separation is a failing case

Decision 6 required it and `the_ay_hash_does_not_move_when_the_beeper_does` is it: two runs of
the identical AY program, one also driving the speaker, with the channel fields asserted
bit-identical and the beeper field asserted different. **The two sources are separate fields of
one `Sample` and are mixed nowhere in this crate** — summing the AY's own three channels would
be a mix too, and is not performed here either.

The hash has moved once, when the envelope divisor and the noise polarity were corrected. That
is recorded beside the constant with what moved, because **the hash did not say the old value
was wrong — a source said that, and the hash said something had changed.** That is the whole of
what `MACHINE.md`'s verification item 4 claims for it.

### The beeper: bit 4 alone, and the research made that ruling stronger

Decision 10 put the beeper in M7 and `ula.rs:528`'s *"open finding, not a deferral"* is closed.
It follows **bit 4 alone**; `MIC` is not modelled, and the reason is now much better than *"a
magnitude with no source"*:

- The `0xFE` port's famous "four levels" is closer to **three** — silence, loud, and loud + 4 %.
  There are two junction drops between the ULA's pin and the speaker on both board issues, so
  **both low levels sit below turn-on and are indistinguishable**, and the two high levels
  differ by 0.14 V of ~3.4 V against the one published measurement.
- **Fuse's ratio is admittedly invented** — its own comment reads *"assume all three tone
  channels together match the beeper volume (ish)"* — and it makes `MIC` asserted **louder**
  where the measurement has it **lower**. Its sign is backwards against the only data there is.
- **The 128 may not sum them at all.** Redrawn 128/+2 schematics show the 128's ULA with
  **separate EAR and MIC pins**, where the 48K shares pin 28 — and the 128 service manual
  repeats the 48K's summing prose verbatim. Unresolved, and this is a 128 emulator.

So a two-level speaker is the model, and what is left out is named rather than left unnamed.

**One datum the frontend needs and nobody had:** the 128's board sums the ULA's sound against
the AY's into the MC1376 through `R112 6K8` against `R132 18K` — contribution as `1/R`, so
**beeper 2.65× the AY**. That is the only sourced number on the relative loudness of the two,
and the mix is the frontend's, so it is recorded here rather than applied here.

### What sound costs on the hot path: nothing, and it is measured

`Ula::tick`, `Ula::contend` and `Ula::advance` are **byte-identical** to their pre-sound selves
— `diff` against `HEAD`, not an assertion. Nothing generates audio per T-state: the chip's
state is a pure function of its registers and elapsed time, so the generator runs late, at a
speaker-bit **change**, an AY register write, or a drain, and never otherwise.

Decision 3 recorded that *"the command to measure this does not exist"* and said what would
settle it: a bench in `crates/spectrum` stepping the real `Ula` over a frame.
**`crates/spectrum/benches/frame.rs` is that command.** Medians, `cargo bench -p spectrum`,
2026-09-01, against a 20,000 µs frame:

| Case | Median | Over `quiet_48k` |
|---|---|---|
| `quiet_48k` — nobody touching sound | 148.0 µs | — |
| `quiet_rendered_48k` | 151.7 µs | +3.7 µs |
| `drained_48k` — one `take_samples` a frame | 154.4 µs | +6.4 µs |
| `border_48k` — 3900 border changes | 167.1 µs | +19.1 µs |
| `border_rendered_48k` | 174.3 µs | +26.3 µs |
| `beeper_48k` — a speaker edge every 18 T-states | 197.4 µs | +49.4 µs |

**`quiet_48k` did not move**, before or after. The heaviest case is 1 % of a frame's budget,
and it is far heavier than real software: a music driver writes the AY about fourteen times a
frame, and a tape load changes the border 25 to 30 times.

### The `BEEP` pitch, measured on this side of the boundary

The frontend measures `BEEP 1,0` through the whole path — keymap, samples, mix, resample, pitch
— at **261.71 Hz against the manual's 261.63**, 0.03 % out. That is an oracle for the machine's
clock that nobody had, and it is worth keeping.

**It agrees with this crate, and the agreement is what caught the one number that did not.** The
end-to-end gate first read 246.65 Hz — 5.7 % flat, very nearly a semitone — and the natural
reading was a T-state error in the beeper's loop. Measured here through the ROM's own `BEEPER`
routine, timing the speaker's edges on the machine's own clock and its own sample stream, with
the ROM's own arithmetic supplying the loop count (`HL = 437500/f - 30.125`):

| | `HL` | half-period | implied | expected | ratio |
|---|---|---|---|---|---|
| middle C | 1642 | 6687.2 T-states | **261.69 Hz** | 261.63 | **1.0002** |
| A above | 963 | 3971.8 T-states | 440.60 Hz | 440.00 | 1.0014 |

Edges are detected at sample granularity, so the resolution is ±32 T-states — about ±0.5 % —
and the agreement is inside it. **A 6 % error is excluded by two orders of magnitude.** Three
things independently agree: instruction timing is graded at 290/290 and 1045/1045 by FUSE and
67/67 by `zexall`, so a systematic T-state error is nearly excluded; a 48K frame is asserted to
be exactly 2184 samples; and `cpu_hz` is cross-checked against its own frame length. Four
measurements against one, and the one was the frontend's own probe: it counted zero crossings
but took its duration from the amplitude envelope, so the 34 ms of the resampler's DC blocker
ringing down after the note stopped were divided in as though they were period. Measured from
the crossing spacing alone, the same buffer reads 261.71 Hz — the figure at the top of this
section, and the one this table implies. **The machine was right, and the rule the episode paid
for is to suspect the measurement before the machine.**

### The border, which arrived mid-milestone

The owner reported a tape load's stripes as wrong; `screen::render` painted one colour for the
frame. **One `OUT (0xFE)` write, one event, two consumers** — the border needs it remembered
until something renders, so it has a record; the speaker turns it into samples immediately, so
it has none. A shared log feeding both was considered and rejected: the two have different
lifetimes (a frame against a consumer's drain schedule) and different overflow semantics, and
coupling them would let a display-shaped bound drop audio.

**The record is bounded by construction** — one slot per rendered row, and a guest cannot create
rows — so there is no allocation sized by guest behaviour, no drop policy, and no failing case
for a policy to have. What is lost instead is stated and gated: several writes inside one row
collapse to the last of them, which is what makes border-multicolour demos out of reach.

**The defect a gate found rather than a design:** a record served only for the frame *running*
shows a frontend a uniform border **every time**, because `run_frame(); render();` renders a few
T-states into the next frame. It passed every test that rendered mid-frame.
`the_frontends_own_loop_shows_the_bands` is named for the call pattern, because the call pattern
was what was wrong.

### A defect this pass found in work it did not write

`snapshot::z80::write` emitted hardware mode **`0`** for every model, so **a 128 snapshot became
a 48K file carrying three of its eight banks.** It is the *"silent last-write-wins"* `M6.md`
refused for duplicate pages, in the one direction nothing guarded — `Spectrum::restore`'s
`ModelMismatch` refuses a 128 image entering a 48K machine, and that asymmetry is why the other
direction went unnoticed. **The information was never missing**: `Snapshot::model` has existed
since M7 for exactly this, and the writer was not reading it. Closed, with the reader half the
round trip needs, and with the bank-set guard `Decision 7` specified — compared against the
**model**'s set rather than the slot map's, and scoped to the 128, because a 48K's three banks
are always addressable and a missing one is visible without it.

One regression on the way, caught by the transcribed vector at byte 88: a first cut iterated
banks in ascending order where the canonical form is **address** order, silently reordering the
page blocks of every 48K file.
